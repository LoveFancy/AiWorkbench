<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.workmate.server.mapper.UpgradeReleaseMapper">

    <resultMap id="BaseResultMap" type="com.workmate.server.entity.UpgradeRelease">
        <id column="id" property="id"/>
        <result column="version" property="version"/>
        <result column="release_type" property="releaseType"/>
        <result column="release_notes" property="releaseNotes"/>
        <result column="download_url" property="downloadUrl"/>
        <result column="platform" property="platform"/>
        <result column="min_version" property="minVersion"/>
        <result column="is_active" property="isActive"/>
        <result column="published_at" property="publishedAt"/>
    </resultMap>

    <select id="findFirstByPlatformAndIsActiveTrue" resultMap="BaseResultMap">
        SELECT * FROM upgrade_releases
        WHERE platform = #{platform} AND is_active = true
        ORDER BY published_at DESC LIMIT 1
    </select>

    <select id="findFirstByPlatformAndVersion" resultMap="BaseResultMap">
        SELECT * FROM upgrade_releases
        WHERE platform = #{platform} AND version = #{version} LIMIT 1
    </select>

    <update id="deactivateByPlatform">
        UPDATE upgrade_releases SET is_active = false
        WHERE platform = #{platform} AND is_active = true
    </update>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO upgrade_releases (version, release_type, release_notes, download_url, platform, min_version, is_active, published_at)
        VALUES (#{version}, #{releaseType}, #{releaseNotes}, #{downloadUrl}, #{platform}, #{minVersion}, #{isActive}, #{publishedAt})
    </insert>

    <update id="update">
        UPDATE upgrade_releases
        SET version = #{version}, release_type = #{releaseType}, release_notes = #{releaseNotes},
            download_url = #{downloadUrl}, platform = #{platform}, min_version = #{minVersion},
            is_active = #{isActive}
        WHERE id = #{id}
    </update>

    <select id="findByPlatform" resultMap="BaseResultMap">
        SELECT * FROM upgrade_releases WHERE platform = #{platform} ORDER BY published_at DESC
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM upgrade_releases ORDER BY published_at DESC
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM upgrade_releases
        <if test="platform != null and platform != ''">
            WHERE platform = #{platform}
        </if>
    </select>

</mapper>
