# ============================================================
# 本地开发环境配置 (spring.profiles.active=localdev)
# ============================================================
# 启动方式:
#   mvn spring-boot:run -Dspring-boot.run.profiles=localdev
#   或
#   java -jar target/xxx.jar --spring.profiles.active=localdev
#   或设置环境变量 SPRING_PROFILES_ACTIVE=localdev
# ============================================================

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3306}/${DB_NAME:workmate_server}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:fxd1993}

workmate:
  # 开发服务器关闭身份校验
  require-user-id: false
  default-user-id: ${DEFAULT_USER_ID:022480}
  local-dev:
    enabled: true
    api-key: "sk-6d7ba36a9d1a4555add56dced146b389"
    models:
      - id: "deepseek-v4-pro"
        name: "DeepSeek V4 Pro"
        description: "DeepSeek 旗舰模型"
        provider: "anthropic"
        base-url: "https://api.deepseek.com/anthropic"
        max-tokens: 128000
        enabled: true
      - id: "deepseek-v4-flash"
        name: "DeepSeek V4 Flash"
        description: "DeepSeek 快速模型"
        provider: "anthropic"
        base-url: "https://api.deepseek.com/anthropic"
        max-tokens: 128000
        enabled: true
