# 客户端：升级检测方案

编辑时间：2026年6月3日

> **关联文档**：对应后端服务设计见 [服务端-workmate升级检测和灰度升级管理.md](../服务端/服务端-workmate升级检测和灰度升级管理.md)。客户端登录方案见 [客户端-登录.md](./客户端-登录.md)。请求经过 EIP 网关，网关自动注入 `X-EIPGW-USERID` Header。

---

## 一、设计思路

应用启动后以**随机延迟**首次检测，之后按**随机抖动的周期**重复检测。检测到更新后**不强制弹窗、不自动下载**，而是在界面展示一个升级图标/角标，由用户主动点击触发下载安装。

### 核心原则

1. **最小侵入**：所有升级代码集中在 `apps/electron/src/upgrade/` 独立目录
2. **随机分散**：首次检测延迟和周期检测间隔均加入随机因子，避免"早高峰"集中请求
3. **用户主导**：静默检测，结果通过图标呈现，下载安装由用户主动触发

### 与现有 auto-updater 的互斥

Proma 已有 `auto-updater.ts`（基于 `electron-updater` 检查 GitHub Release）。WorkMate 升级检测走服务端 API。**两套升级系统互斥**，按 `workmate.json` 是否配置 `serverUrl` 决定启用哪一套：

| `workmate.serverUrl` | 启用模块 | 升级源 |
|---------------------|---------|--------|
| 已配置 | `registerUpgradeCheck()` | WorkMate Server |
| 未配置 | 原有 `initAutoUpdater()` | GitHub Release |

---

## 二、目录结构

所有升级检测代码集中在 `apps/electron/src/upgrade/` 目录：

```
apps/electron/src/upgrade/
├── index.ts              # 统一导出入口 + 启动/关闭函数
├── types.ts              # 所有类型定义（自包含）
├── upgrade-service.ts    # 升级检测服务（网络请求 + 随机定时 + 结果缓存）
├── ipc-handlers.ts       # IPC 处理器注册（export registerUpgradeIpcHandlers）
├── preload-bridge.ts     # preload 桥接注册（export registerUpgradePreloadBridge）
├── atoms.ts              # Jotai 状态
└── UpgradeIndicator.tsx  # 升级提示图标组件
```

---

## 三、随机检测策略

### 3.1 为什么需要随机分散

如果不加随机，所有用户早上 9:00 打开客户端，都在 9:00:10 发起首次检测，12 小时后又在 21:00:10 同时检测——服务端瞬间承受大量并发。加入随机后，检测时间被分散到不同的时间窗口内。

### 3.2 随机算法

```
首次检测延迟:
  delay = random(10_000, 3_600_000)    // 10秒 ~ 60分钟

周期检测间隔:
  interval = BASE_INTERVAL + random(0, JITTER_RANGE)
  BASE_INTERVAL = 6 * 3600 * 1000      // 6小时
  JITTER_RANGE  = 2 * 3600 * 1000      // ±2小时随机抖动

  即：实际间隔在 6h ~ 8h 之间随机分布
```

**效果**：假设 100 个用户同一时刻启动，首次检测会分散在 10s~60min 内。后续周期检测，每个人的节奏也不同（有人 6.3h，有人 7.7h），最终各用户的检测时间点会完全发散。

### 3.3 行为矩阵

| 场景 | 首次检测延迟 | 后续检测间隔 |
|------|-------------|-------------|
| 正常启动 | 10s ~ 60min 随机 | 6h ~ 8h 随机 |
| 登录成功 | 10s 后立即检查一次 | 不影响周期计时器 |
| 用户手动点击刷新 | 立即检查 | 不影响周期计时器 |
| 登出 | — | 不触发检测 |
| `serverUrl` 未配置 | 不启动检测 | — |

---

## 四、用户主动升级流程

```
1. 后台静默检测 → 发现新版本
          ↓
2. 渲染进程展示升级图标/角标（UpgradeIndicator）
   不弹窗，不打断用户
          ↓
3. 用户注意到图标 → 点击
          ↓
4. 弹出更新详情对话框：
   - 版本号 (currentVersion → latestVersion)
   - 发布说明 (releaseNotes)
   - releaseType: "升级" 或 "回退"
   - [立即下载] [稍后再说]
          ↓
5. 用户点击 [立即下载]
          ↓
6. 调用 shell.openExternal(downloadUrl)
   打开浏览器下载安装包
   (或调用 Electron downloadURL 下载)
          ↓
7. 用户手动运行安装包完成升级
```

### forceUpdate 的特殊处理

当 `forceUpdate=true` 时，`UpgradeIndicator` 不可关闭/忽略，点击后仅显示 [立即下载] 按钮，无 [稍后再说] 选项。此时图标持续显示，直到用户完成升级。

---

## 五、接口定义

### 5.1 客户端 → 服务端

```
GET /workmate/upgrade/check?currentVersion=X&platform=Y
Cookie: EIPGW-TOKEN=<jwt>
```

**请求参数**：

| 参数 | 类型 | 说明 |
|------|------|------|
| `currentVersion` | string | 当前客户端版本号 |
| `platform` | string | 平台：win32 / darwin / linux |

### 5.2 服务端 → 客户端

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "hasUpdate": true,
    "forceUpdate": false,
    "releaseType": "UPGRADE",
    "latestVersion": "1.2.0",
    "downloadUrl": "https://example.com/releases/1.2.0/installer.exe",
    "releaseNotes": "修复了若干已知问题，优化了性能",
    "minVersion": "1.0.0",
    "hint": "有新版本可用，建议更新",
    "publishedAt": "2026-05-20T10:00:00Z"
  },
  "timestamp": 1717300000000
}
```

| 字段 | 说明 |
|------|------|
| `hasUpdate` | 是否有更新（包括升级和回退） |
| `forceUpdate` | 是否强制升级（用户必须升级才能继续使用） |
| `releaseType` | `UPGRADE` 升级 / `ROLLBACK` 回退 |
| `latestVersion` | 目标版本号 |
| `minVersion` | 最低允许的当前版本（低于此版本必须升级） |
| `downloadUrl` | 安装包下载地址 |
| `releaseNotes` | 发布说明 |
| `hint` | 提示文案 |

### 5.3 服务端检测逻辑（参考，不在客户端范围内）

```
GET /workmate/upgrade/check
    ↓
1. 查 ACTIVE 策略 → 无 → hasUpdate=false
                 → 有 → 进入步骤 2
2. 白名单校验（基于策略已执行阶段的规则）：
   - 全量阶段（rules 为空）→ 全员可用
   - 非全量阶段 → 合并已执行阶段规则，匹配用户工号
     → 不匹配 → hasUpdate=false
3. 版本比较：
   - UPGRADE: targetVersion > currentVersion → hasUpdate=true
   - ROLLBACK: targetVersion < currentVersion → hasUpdate=true
4. 返回 { hasUpdate, releaseType, latestVersion, downloadUrl, ... }
```

### 5.4 端侧二次校验

收到 `hasUpdate=true` 后，客户端必须校验版本方向的正确性：

```
releaseType = 'UPGRADE':
  latestVersion > currentVersion → 有效
  latestVersion ≤ currentVersion → 忽略（异常数据）

releaseType = 'ROLLBACK':
  latestVersion < currentVersion → 有效
  latestVersion ≥ currentVersion → 忽略（异常数据）
```

---

## 六、各文件详细设计

### 6.1 `types.ts` — 类型定义

```typescript
// apps/electron/src/upgrade/types.ts

export interface UpgradeCheckResult {
  hasUpdate: boolean
  forceUpdate: boolean
  releaseType: 'UPGRADE' | 'ROLLBACK' | null
  currentVersion: string
  latestVersion: string | null
  minVersion: string | null
  downloadUrl: string | null
  hint: string | null
  releaseNotes: string | null
  publishedAt?: string
}

/** 升级图标展示状态 */
export interface UpgradeIndicatorState {
  visible: boolean
  result: UpgradeCheckResult | null
}
```

### 6.2 `upgrade-service.ts` — 升级检测服务

```
函数列表：
├── initUpgradeService(configDir, serverUrl?)   — 初始化（注入配置路径）
├── checkForUpgrade(): Promise<UpgradeCheckResult> — 发起检测请求
├── startPeriodicCheck()                        — 启动随机定时检测
├── stopPeriodicCheck()                         — 停止定时检测
├── getLastResult(): UpgradeCheckResult | null  — 获取最近一次检测结果
└── isVersionNewer(a, b): boolean               — SemVer 比较
```

```typescript
// apps/electron/src/upgrade/upgrade-service.ts

import { app } from 'electron'
import { getToken } from '../auth'
import type { UpgradeCheckResult } from './types'

// ===== 随机参数 =====
const FIRST_CHECK_DELAY_MIN = 10_000       // 10 秒
const FIRST_CHECK_DELAY_MAX = 3_600_000    // 60 分钟
const BASE_INTERVAL_MS = 6 * 3_600_000     // 6 小时
const JITTER_RANGE_MS = 2 * 3_600_000      // 2 小时抖动

let _serverUrl: string | null = null
let _checkTimer: ReturnType<typeof setInterval> | null = null
let _lastResult: UpgradeCheckResult | null = null
let _onUpdateFound: ((result: UpgradeCheckResult) => void) | null = null

/** 初始化升级服务（由 main/index.ts 调用） */
export function initUpgradeService(serverUrl?: string): void {
  _serverUrl = serverUrl ?? null
}

/** 注册更新回调（检测到更新时通知渲染进程） */
export function onUpdateFound(callback: (result: UpgradeCheckResult) => void): void {
  _onUpdateFound = callback
}

// ===== SemVer 比较 =====

export function isVersionNewer(a: string, b: string): boolean {
  const pa = a.split('.').map(n => parseInt(n, 10) || 0)
  const pb = b.split('.').map(n => parseInt(n, 10) || 0)
  for (let i = 0; i < 3; i++) {
    const diff = (pa[i] ?? 0) - (pb[i] ?? 0)
    if (diff !== 0) return diff > 0
  }
  return false
}

// ===== 随机工具 =====

function randomBetween(min: number, max: number): number {
  return min + Math.floor(Math.random() * (max - min))
}

// ===== 核心检测逻辑 =====

export async function checkForUpgrade(): Promise<UpgradeCheckResult> {
  const currentVersion = app.getVersion()
  const empty: UpgradeCheckResult = {
    hasUpdate: false, forceUpdate: false, releaseType: null,
    currentVersion, latestVersion: null, minVersion: null,
    downloadUrl: null, hint: null, releaseNotes: null,
  }

  if (!_serverUrl) return empty

  const token = getToken()
  if (!token) return empty

  const baseUrl = _serverUrl.replace(/\/+$/, '')
  const platform = process.platform
  const url = `${baseUrl}/workmate/upgrade/check`
    + `?currentVersion=${encodeURIComponent(currentVersion)}`
    + `&platform=${encodeURIComponent(platform)}`

  try {
    const response = await fetch(url, {
      headers: { 'Cookie': `EIPGW-TOKEN=${token}` },
      signal: AbortSignal.timeout(10_000),
    })

    if (!response.ok) return empty

    const json = await response.json()
    if (json.code !== 0 || !json.data) return empty

    const data = json.data
    const result: UpgradeCheckResult = {
      hasUpdate: Boolean(data.hasUpdate),
      forceUpdate: Boolean(data.forceUpdate),
      releaseType: data.releaseType ?? null,
      currentVersion,
      latestVersion: data.latestVersion ?? null,
      minVersion: data.minVersion ?? null,
      downloadUrl: data.downloadUrl ?? null,
      hint: data.hint ?? null,
      releaseNotes: data.releaseNotes ?? null,
      publishedAt: data.publishedAt,
    }

    // 端侧二次校验
    if (result.hasUpdate && result.latestVersion) {
      const valid = validateVersionDirection(
        result.releaseType, result.latestVersion, currentVersion,
      )
      if (!valid) {
        result.hasUpdate = false
        result.releaseType = null
      }
    }

    _lastResult = result

    // 检测到更新时通知
    if (result.hasUpdate && _onUpdateFound) {
      _onUpdateFound(result)
    }

    return result
  } catch {
    return empty
  }
}

function validateVersionDirection(
  releaseType: string | null,
  latestVersion: string,
  currentVersion: string,
): boolean {
  if (releaseType === 'UPGRADE') {
    return isVersionNewer(latestVersion, currentVersion)
  }
  if (releaseType === 'ROLLBACK') {
    return !isVersionNewer(latestVersion, currentVersion) && latestVersion !== currentVersion
  }
  return false
}

// ===== 定时检测 =====

export function startPeriodicCheck(): void {
  if (_checkTimer) return
  if (!_serverUrl) return

  // 首次检测：随机延迟 10s ~ 30min
  const firstDelay = randomBetween(FIRST_CHECK_DELAY_MIN, FIRST_CHECK_DELAY_MAX)
  console.log(`[升级检测] 首次检测将在 ${Math.round(firstDelay / 1000)}s 后执行`)

  setTimeout(() => {
    void checkForUpgrade()
    // 首次检测后启动周期定时器
    scheduleNext()
  }, firstDelay)
}

function scheduleNext(): void {
  if (!_serverUrl) return
  const interval = BASE_INTERVAL_MS + randomBetween(0, JITTER_RANGE_MS)
  _checkTimer = setTimeout(() => {
    void checkForUpgrade()
    scheduleNext() // 递归调度下一次
  }, interval)
}

export function stopPeriodicCheck(): void {
  if (_checkTimer) {
    clearTimeout(_checkTimer)
    _checkTimer = null
  }
  _lastResult = null
}

export function getLastResult(): UpgradeCheckResult | null {
  return _lastResult
}
```

### 6.3 `ipc-handlers.ts` — IPC 处理器注册

```typescript
// apps/electron/src/upgrade/ipc-handlers.ts

import { ipcMain, shell } from 'electron'
import { checkForUpgrade, getLastResult } from './upgrade-service'

const CHANNELS = {
  CHECK: 'upgrade:check',
  GET_LAST_RESULT: 'upgrade:get-last-result',
  OPEN_DOWNLOAD: 'upgrade:open-download',
} as const

/**
 * 注册 upgrade 相关的 IPC handler。
 *
 * 调用方式（main/index.ts 中加一行）：
 *   import { registerUpgradeIpcHandlers } from '../upgrade'
 *   registerUpgradeIpcHandlers()
 */
export function registerUpgradeIpcHandlers(): void {
  ipcMain.handle(CHANNELS.CHECK, async () => {
    return checkForUpgrade()
  })

  ipcMain.handle(CHANNELS.GET_LAST_RESULT, () => {
    return getLastResult()
  })

  ipcMain.handle(CHANNELS.OPEN_DOWNLOAD, async (_event, url: string) => {
    await shell.openExternal(url)
    return { success: true }
  })
}

export { CHANNELS as UPGRADE_IPC_CHANNELS }
```

### 6.4 `preload-bridge.ts` — Preload 桥接注册

主进程检测到更新后通过 `webContents.send` 通知渲染进程；渲染进程也可通过 IPC 主动查询。

```typescript
// apps/electron/src/upgrade/preload-bridge.ts

import { contextBridge, ipcRenderer } from 'electron'
import { UPGRADE_IPC_CHANNELS } from './ipc-handlers'

export interface UpgradeElectronAPI {
  checkForUpdate: () => Promise<{
    hasUpdate: boolean; forceUpdate: boolean; releaseType: string | null
    currentVersion: string; latestVersion: string | null
    downloadUrl: string | null; releaseNotes: string | null; hint: string | null
  }>
  getLastResult: () => Promise<unknown>
  openDownload: (url: string) => Promise<{ success: boolean }>
  /** 监听主进程推送的更新通知 */
  onUpdateFound: (callback: (result: unknown) => void) => () => void
}

/**
 * 调用方式（preload/index.ts 中加一行）：
 *   import { registerUpgradePreloadBridge } from '../upgrade'
 *   registerUpgradePreloadBridge()
 */
export function registerUpgradePreloadBridge(): void {
  contextBridge.exposeInMainWorld('electronAPI', {
    upgrade: {
      checkForUpdate: () =>
        ipcRenderer.invoke(UPGRADE_IPC_CHANNELS.CHECK),
      getLastResult: () =>
        ipcRenderer.invoke(UPGRADE_IPC_CHANNELS.GET_LAST_RESULT),
      openDownload: (url: string) =>
        ipcRenderer.invoke(UPGRADE_IPC_CHANNELS.OPEN_DOWNLOAD, url),
      onUpdateFound: (callback: (result: unknown) => void) => {
        const handler = (_event: Electron.IpcRendererEvent, result: unknown) => callback(result)
        ipcRenderer.on('workmate:upgrade-found', handler)
        return () => ipcRenderer.removeListener('workmate:upgrade-found', handler)
      },
    } satisfies UpgradeElectronAPI,
  })
}
```

### 6.5 `atoms.ts` — Jotai 状态

```typescript
// apps/electron/src/upgrade/atoms.ts

import { atom } from 'jotai'
import type { UpgradeCheckResult, UpgradeIndicatorState } from './types'

export const upgradeResultAtom = atom<UpgradeCheckResult | null>(null)

export const upgradeIndicatorAtom = atom<UpgradeIndicatorState>({
  visible: false,
  result: null,
})

/** 是否有可用更新 */
export const hasUpdateAtom = atom((get) => {
  const result = get(upgradeResultAtom)
  return result?.hasUpdate ?? false
})
```

### 6.6 `UpgradeIndicator.tsx` — 升级提示图标

```
UpgradeIndicator 组件：

┌─────────────────────────────────┐
│  🔔 新版本 v1.2.0 可用          │  ← 顶部/侧边栏图标 + 角标
│     点击查看详情                │
└─────────────────────────────────┘

点击后弹出详情：

┌──────────────────────────────────────┐
│  发现新版本                          │
│                                      │
│  当前版本：1.0.0                     │
│  最新版本：1.2.0                     │
│                                      │
│  更新内容：                          │
│  - 修复了若干已知问题                │
│  - 优化了性能                        │
│                                      │
│       [稍后再说]    [立即下载]        │
└──────────────────────────────────────┘

点击 [立即下载] → 调用 window.electronAPI.upgrade.openDownload(downloadUrl)
```

**forceUpdate 模式**：不显示 [稍后再说] 按钮，图标不可关闭。

### 6.7 `index.ts` — 统一导出入口

```typescript
// apps/electron/src/upgrade/index.ts

// 类型
export type { UpgradeCheckResult, UpgradeIndicatorState } from './types'

// 服务层
export {
  initUpgradeService,
  checkForUpgrade,
  startPeriodicCheck,
  stopPeriodicCheck,
  getLastResult,
  isVersionNewer,
  onUpdateFound,
} from './upgrade-service'

// IPC 注册
export { registerUpgradeIpcHandlers, UPGRADE_IPC_CHANNELS } from './ipc-handlers'

// Preload 注册
export { registerUpgradePreloadBridge } from './preload-bridge'
export type { UpgradeElectronAPI } from './preload-bridge'

// 状态
export {
  upgradeResultAtom,
  upgradeIndicatorAtom,
  hasUpdateAtom,
} from './atoms'

// 组件
export { UpgradeIndicator } from './UpgradeIndicator'
```

---

## 七、主进程通知机制

主进程检测到更新后，通过以下方式通知渲染进程：

```typescript
// upgrade-service.ts 中 startPeriodicCheck() 调用 checkForUpgrade()
// 检测到更新时：
import { BrowserWindow } from 'electron'

_onUpdateFound = (result: UpgradeCheckResult) => {
  // 通知所有窗口
  BrowserWindow.getAllWindows().forEach(win => {
    win.webContents.send('workmate:upgrade-found', result)
  })
}
```

渲染进程通过 `UpgradeIndicator` 组件监听 `window.electronAPI.upgrade.onUpdateFound()` 来更新 UI。

---

## 八、与现有 auto-updater 的互斥实现

在 `main/index.ts` 中（通过 workmate-init 或直接调用）：

```typescript
import { initUpgradeService, startPeriodicCheck } from '../upgrade'

// 读取 workmate.json 判断是否启用企业升级
const workmateConfig = readWorkmateConfig()

if (workmateConfig.serverUrl) {
  // 企业部署：启用 WorkMate 升级检测（随机分散 + 用户主动升级）
  initUpgradeService(workmateConfig.serverUrl)
  startPeriodicCheck()
  // 注意：不启动原有 auto-updater
} else {
  // 开源模式：走 GitHub Release
  initAutoUpdater(win)
}
```

---

## 九、URL 约定

- `serverUrl` **不包含**末尾 `/`，**不包含** `/workmate` 前缀
- 内部拼接：`${serverUrl}/workmate/upgrade/check`，并 `replace(/\/+$/, '')` 兜底
- 示例：
  - 配置：`"serverUrl": "https://workmate-server.example.com"`
  - 实际请求：`https://workmate-server.example.com/workmate/upgrade/check?currentVersion=1.0.0&platform=win32`

---

## 十、安全注意事项

| 项 | 措施 |
|----|------|
| 版本方向校验 | 端侧二次校验 `releaseType` 与版本号方向一致性 |
| 白名单 | 服务端根据策略阶段白名单匹配，不在名单内不推送 |
| 强制升级 | `forceUpdate=true` 时 `UpgradeIndicator` 不可关闭 |
| 下载链接 | 服务端统一下发，避免中间人篡改（经 EIP 网关保护） |

---

## 十一、文件变更清单

### 新增文件（全部在 `apps/electron/src/upgrade/` 下）

```
新增目录：apps/electron/src/upgrade/
├── index.ts              # 统一导出入口
├── types.ts              # 类型定义
├── upgrade-service.ts    # 升级检测服务（随机检测 + 结果缓存）
├── ipc-handlers.ts       # IPC 处理器注册
├── preload-bridge.ts     # preload 桥接注册
├── atoms.ts              # Jotai 状态
└── UpgradeIndicator.tsx  # 升级提示图标组件
```

**共 7 个新增文件，0 个散布到其他目录。**

### 修改原有文件（每个文件仅 1~3 行）

| 文件 | 修改内容 | 修改量 |
|------|---------|--------|
| `apps/electron/src/main/index.ts` | 加 2 行：`import { initUpgradeService, startPeriodicCheck, registerUpgradeIpcHandlers, onUpdateFound } from '../upgrade'` + 调用 | +2~4 行 |
| `apps/electron/src/preload/index.ts` | 加 1 行：`import { registerUpgradePreloadBridge } from '../upgrade'` + 调用 | +1 行 |
| `apps/electron/src/renderer/App.tsx` | 加 1 行：`import { UpgradeIndicator } from '../upgrade'` + JSX 引用 | +1 行 |

**原有文件修改总量：约 3 个文件，每个文件不超过 4 行。**

### 无需修改的文件

| 文件 | 原方案操作 | 新方案 |
|------|-----------|--------|
| `apps/electron/src/main/lib/updater/auto-updater.ts` | 可能修改 | **不动** |
| `apps/electron/src/types/settings.ts` | 修改 | **不动** |
| `apps/electron/src/main/ipc.ts` | 修改 | **不动** |
| `apps/electron/src/main/lib/workmate-init.ts` | 新增文件 | **不动**（互斥逻辑收口到 `main/index.ts`） |
| `apps/electron/src/types/workmate.ts` | 新增 | **不动**（类型自包含在 `upgrade/types.ts`） |

---

*如有任何疑问，请联系信息技术部运营管理室AI研发效能管理团队*
