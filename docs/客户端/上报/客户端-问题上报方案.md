# 客户端 — 问题上报方案

编辑时间：2026年6月17日

> **关联文档**：[问题上报方案（服务端）](./问题上报方案.md)

---

## 目录

- [一、需求概述](#一需求概述)
- [二、服务端接口摘要](#二服务端接口摘要)
- [三、交互与 UI 设计](#三交互与-ui-设计)
- [四、项目结构](#四项目结构)
- [五、实现细节](#五实现细节)

---

## 一、需求概述

在客户端左侧边栏"使用手册"按钮下方新增"问题反馈"按钮，点击后弹出问题反馈弹窗，支持用户填写问题描述并上传多张截图，通过 HTTP multipart 提交到服务端。

**核心原则**：所有新增代码集中在独立的文件夹中，对现有文件的改动最小化（每个文件仅 1-2 行 import + 1 行调用/挂载）。

### 核心流程

```
用户点击左侧边栏「问题反馈」
  → 弹出问题反馈弹窗
  → 输入问题描述 + 选择多张截图（支持预览/删除）
  → 点击提交
  → 主进程构建 FormData，调用 httpUpload()
  → POST /workmate/api/issues
  → 成功 → Toast 提示 + 关闭弹窗
  → 失败 → Toast 提示错误
```

---

## 二、服务端接口摘要

参考：[问题上报方案（服务端）](./问题上报方案.md#四api-接口设计)

### 2.1 提交问题

```
POST /workmate/api/issues
Content-Type: multipart/form-data
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `description` | String | 是 | 问题描述，1-5000 字符 |
| `files` | MultipartFile[] | 否 | 截图文件（支持多张），顺序即数组顺序 |
| `clientVersion` | String | 否 | 客户端版本号 |
| `clientPlatform` | String | 否 | 客户端平台 |

**成功响应**（200）：

```json
{
  "code": 0,
  "message": "提交成功",
  "data": { "id": 1, "status": "PENDING", "imageCount": 3, "createdAt": "..." }
}
```

### 2.2 接口特性

- 无需管理台权限，走客户端接口路径 `/workmate/api/issues`
- Cookie（EIPGW-TOKEN）由 `hteip-client` 自动注入
- 图片访问接口 `GET /workmate/api/issues/images/{fileName}` 无需鉴权

---

## 三、交互与 UI 设计

### 3.1 按钮入口

位置：`LeftSidebar.tsx` — 底部"使用手册"按钮下方。

```
┌─────────────────┐
│  ...            │
│  📖 使用手册     │  ← 现有
│  🐛 问题反馈    │  ← 新增
│  👤 用户菜单     │
└─────────────────┘
```

### 3.2 问题反馈弹窗

以 Dialog 浮窗形式展示（参考 `SettingsDialog` 模式，轻遮罩 + 居中弹窗），尺寸约 `520×560px`。

```
┌──────────────────────────────────────────────┐
│  问题反馈                              [✕]   │
├──────────────────────────────────────────────┤
│                                              │
│  问题描述 *                                  │
│  ┌────────────────────────────────────────┐  │
│  │ 请输入问题描述（1-5000 字）               │  │
│  │                                        │  │
│  │                                        │  │
│  └────────────────────────────────────────┘  │
│  <span>128/5000</span>                       │
│                                              │
│  截图附件（可选，最多 9 张）                    │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐       │
│  │ 图1  │ │ 图2  │ │ 图3  │ │  +   │       │
│  │ [✕] │ │ [✕] │ │ [✕] │ │ 添加  │       │
│  └──────┘ └──────┘ └──────┘ └──────┘       │
│                                              │
│  提示：图片按添加顺序排列                       │
│                                              │
│  ┌────────────────────────────────────────┐  │
│  │              提交反馈                    │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

### 3.3 交互规则

| 场景 | 行为 |
|------|------|
| 描述为空 | 提交按钮禁用 + placeholder 提示 |
| 描述超过 5000 字 | 输入截断 + 字符计数器标红 |
| 已选 9 张图片 | "添加"按钮隐藏 |
| 图片格式不对 | 选择时过滤，仅允许 `.png/.jpg/.jpeg/.gif/.webp` |
| 单张超过 10MB | 弹 toast 提示"图片过大，单张不能超过 10MB" |
| 提交中 | 按钮显示 loading，不允许重复提交 |
| 提交成功 | toast 成功 + 关闭弹窗 + 清空表单 |
| 提交失败 | toast 错误信息，表单保留，允许重试 |
| 关闭弹窗（✕ 或遮罩） | 若有已填写内容，弹 confirm 确认 |

---

## 四、项目结构

### 4.1 新增文件（全部独立文件夹）

```
apps/electron/src/
├── main/lib/issue-report/                       # ← 新增文件夹
│   ├── index.ts                                 # barrel + 自注册（import 即完成 IPC 注册）
│   └── issue-report-service.ts                  # 核心逻辑：构建 FormData + HTTP 上传
│
└── renderer/components/issue-report/            # ← 新增文件夹
    ├── index.ts                                 # barrel export
    ├── IssueReportDialog.tsx                     # 弹窗组件（描述输入 + 图片选择/预览/删除 + 提交）
    └── IssueReportButton.tsx                     # 侧边栏按钮组件（独立封装）
```

共新增 **5 个文件**，分布在 **2 个独立文件夹**中。

### 4.2 现有文件改动（各 ≤ 2 行）

| 文件 | 改动 | 行数 |
|------|------|------|
| `main/index.ts` | `import './lib/issue-report'` | +1 行 |
| `preload/index.ts` | 类型声明 + IPC 转发实现 | +5 行 |
| `App.tsx` | `import { IssueReportDialog }` + `<IssueReportDialog />` | +2 行 |
| `LeftSidebar.tsx` | `import { IssueReportButton }` + `<IssueReportButton />` | +2 行 |

**无需修改的文件**：`hteip-client.ts`（复用 `httpUpload`）、`settings-tabs.tsx`（问题反馈不走设置面板）、任何 UI 组件库。

---

## 五、实现细节

### 5.1 主进程 — issue-report/service.ts

```ts
// apps/electron/src/main/lib/issue-report/issue-report-service.ts

import { app } from 'electron'
import { getUserProfile } from '../user-profile-service'
import { httpUpload, resolveApiBase } from '../../../shared/hteip-client'
import type { HttpResponse } from '../../../shared/hteip-client'

const UPLOAD_PATH = '/workmate/api/issues'

export interface IssueSubmitInput {
  description: string
  files: Array<{
    name: string       // 原始文件名
    data: Buffer       // 文件内容
    mimeType: string   // 如 image/png
  }>
}

interface UploadApiResponse {
  code: number
  message: string
  data?: {
    id: number
    status: string
    imageCount: number
    createdAt: string
  }
}

export interface IssueSubmitSuccess {
  success: true
  id: number
  imageCount: number
}

export interface IssueSubmitError {
  success: false
  error: string
}

export type IssueSubmitResult = IssueSubmitSuccess | IssueSubmitError

export const ISSUE_SUBMIT_IPC_CHANNEL = 'issue:submit'

function resolveJobId(): string {
  const profile = getUserProfile()
  return profile.userName?.trim() || 'unknown'
}

export async function submitIssue(input: IssueSubmitInput): Promise<IssueSubmitResult> {
  const platformMap: Record<string, string> = {
    win32: 'windows', darwin: 'mac', linux: 'linux'
  }

  const formData = new FormData()
  formData.append('description', input.description)
  formData.append('clientVersion', app.getVersion())
  formData.append('clientPlatform', platformMap[process.platform] ?? process.platform)

  // 按顺序追加图片
  for (const file of input.files) {
    formData.append(
      'files',
      new Blob([new Uint8Array(file.data)], { type: file.mimeType }),
      file.name
    )
  }

  console.log('[问题上报] POST %s%s (描述: %d 字, 图片: %d 张)',
    resolveApiBase(), UPLOAD_PATH,
    input.description.length,
    input.files.length
  )

  const res: HttpResponse<UploadApiResponse> = await httpUpload<UploadApiResponse>(
    UPLOAD_PATH,
    { formData },
  )

  if (res.ok && res.data?.code === 0 && res.data.data) {
    console.log('[问题上报] 提交成功: id=%d', res.data.data.id)
    return {
      success: true,
      id: res.data.data.id,
      imageCount: res.data.data.imageCount,
    }
  }

  const errorMsg = res.error || res.data?.message || `提交失败 (HTTP ${res.status})`
  console.error('[问题上报] 提交失败:', errorMsg)
  return { success: false, error: errorMsg }
}
```

### 5.2 主进程 — issue-report/index.ts（自注册入口）

```ts
// apps/electron/src/main/lib/issue-report/index.ts
//
// 副作用导入：import 本模块即自动完成 IPC handler 注册。
// 调用方只需在 main/index.ts 中加一行：
//   import './lib/issue-report'

import { ipcMain } from 'electron'
import { submitIssue, ISSUE_SUBMIT_IPC_CHANNEL } from './issue-report-service'
import type { IssueSubmitInput } from './issue-report-service'

ipcMain.handle(
  ISSUE_SUBMIT_IPC_CHANNEL,
  async (_event, input: IssueSubmitInput) => {
    try {
      return await submitIssue(input)
    } catch (err) {
      const message = err instanceof Error ? err.message : '问题提交失败'
      console.error('[IPC] issue:submit 失败:', message)
      return { success: false, error: message }
    }
  },
)
```

> **设计说明**：采用"副作用导入"模式 — 文件在 import 时自动执行 `ipcMain.handle` 注册。`main/index.ts` 只需 `import './lib/issue-report'` 即可，无需手动调用注册函数。

### 5.3 main/index.ts 改动

```ts
// 在现有 import 区域追加一行：
import './lib/issue-report'
```

> 位置参考：放在 `import './lib/log-upload-ipc'` 附近。

### 5.4 预加载 — preload/index.ts 改动

**类型声明**（在接口定义区域的 `uploadSystemLog` 之后新增）：

```ts
/** 提交问题反馈 */
submitIssue: (input: {
  description: string
  files: Array<{ name: string; data: number[]; mimeType: string }>
}) => Promise<
  | { success: true; id: number; imageCount: number }
  | { success: false; error: string }
>
```

**实现**（在 `uploadSystemLog` 实现之后新增）：

```ts
submitIssue: (input) => {
  return ipcRenderer.invoke('issue:submit', input)
},
```

> **Buffer 序列化说明**：渲染进程通过 IPC 传递文件时，Buffer 会被 Electron 自动序列化为 `number[]`，主进程接收后使用 `Buffer.from(numbers)` 还原。

### 5.5 渲染进程 — IssueReportButton.tsx（侧边栏按钮）

```tsx
// apps/electron/src/renderer/components/issue-report/IssueReportButton.tsx
//
// 独立封装的侧边栏按钮组件，LeftSidebar 只需 import + 挂载。

import * as React from 'react'
import { Bug } from 'lucide-react'
import { Tooltip, TooltipTrigger, TooltipContent } from '@/components/ui/tooltip'
import { useSetAtom } from 'jotai'
import { issueReportOpenAtom } from '@/atoms/issue-report'

export function IssueReportButton(): React.ReactElement {
  const setOpen = useSetAtom(issueReportOpenAtom)

  return (
    <Tooltip>
      <TooltipTrigger asChild>
        <button
          onClick={() => setOpen(true)}
          className="flex items-center gap-3 min-w-0 px-3 py-2 rounded-[10px] transition-colors titlebar-no-drag text-foreground/70 hover:bg-foreground/[0.04] hover:text-foreground"
        >
          <Bug className="size-5 flex-shrink-0" />
          <span className="flex-1 text-sm truncate text-left">问题反馈</span>
        </button>
      </TooltipTrigger>
      <TooltipContent side="top">提交问题反馈</TooltipContent>
    </Tooltip>
  )
}
```

### 5.6 LeftSidebar.tsx 改动

在"使用手册"按钮下方新增一行：

```tsx
import { IssueReportButton } from '@/components/issue-report'

// 在"使用手册"按钮的 </Tooltip> 之后，用户菜单之前：
<IssueReportButton />
```

### 5.7 渲染进程 — IssueReportDialog.tsx（弹窗组件）

核心实现要点：

- 使用 `@radix-ui/react-dialog` 原语（与 SettingsDialog 一致）
- 通过 Jotai `issueReportOpenAtom` 控制开关
- 客户端侧校验（提交按钮联动）：
  - 描述非空 + ≤ 5000 字符
  - 图片格式白名单：`.png/.jpg/.jpeg/.gif/.webp`
  - 图片大小：单张 ≤ 10MB
  - 图片数量：最多 9 张
- 图片选择：`<input type="file" multiple accept="image/png,image/jpeg,image/gif,image/webp">`
- 图片预览：读取为 URL 缩略图，每张带 ✕ 删除按钮
- 提交时：将 File 读取为 ArrayBuffer → 通过 IPC 传递 → 主进程构建 FormData → httpUpload
- Toast 反馈：使用 `sonner`

```tsx
// apps/electron/src/renderer/components/issue-report/IssueReportDialog.tsx

import * as React from 'react'
import { useAtom } from 'jotai'
import { X, Bug, Loader2, Plus, Trash2 } from 'lucide-react'
import * as DialogPrimitive from '@radix-ui/react-dialog'
import { Button } from '@/components/ui/button'
import { toast } from 'sonner'
import { issueReportOpenAtom } from '@/atoms/issue-report'

const MAX_DESC_LENGTH = 5000
const MAX_IMAGES = 9
const MAX_FILE_SIZE = 10 * 1024 * 1024 // 10MB
const ALLOWED_TYPES = ['image/png', 'image/jpeg', 'image/gif', 'image/webp']
const ALLOWED_EXTENSIONS = ['.png', '.jpg', '.jpeg', '.gif', '.webp']

interface ImageEntry {
  file: File
  previewUrl: string
}

export function IssueReportDialog(): React.ReactElement {
  const [open, setOpen] = useAtom(issueReportOpenAtom)
  const [description, setDescription] = React.useState('')
  const [images, setImages] = React.useState<ImageEntry[]>([])
  const [submitting, setSubmitting] = React.useState(false)

  const hasContent = description.trim().length > 0 || images.length > 0

  // 关闭时若有内容则确认
  const handleOpenChange = React.useCallback(
    (next: boolean) => {
      if (!next && hasContent) {
        if (!window.confirm('有未提交的内容，确定关闭吗？')) return
      }
      setOpen(next)
      if (!next) {
        // 延迟清空，避免关闭动画时看到空表单
        setTimeout(() => resetForm(), 200)
      }
    },
    [hasContent, setOpen],
  )

  const resetForm = () => {
    setDescription('')
    // 释放预览 URL
    images.forEach((img) => URL.revokeObjectURL(img.previewUrl))
    setImages([])
  }

  const canSubmit = description.trim().length > 0 && !submitting

  const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    const selectedFiles = Array.from(e.target.files ?? [])
    // 格式 + 大小校验
    const valid: ImageEntry[] = []
    for (const f of selectedFiles) {
      const ext = '.' + f.name.split('.').pop()?.toLowerCase()
      if (!ALLOWED_TYPES.includes(f.type) && !ALLOWED_EXTENSIONS.includes(ext)) {
        toast.error(`不支持的图片格式: ${f.name}`)
        continue
      }
      if (f.size > MAX_FILE_SIZE) {
        toast.error(`图片过大（单张不超过 10MB）: ${f.name}`)
        continue
      }
      valid.push({ file: f, previewUrl: URL.createObjectURL(f) })
    }
    const remaining = MAX_IMAGES - images.length
    if (valid.length > remaining) {
      toast.error(`最多还能添加 ${remaining} 张图片`)
      setImages((prev) => [...prev, ...valid.slice(0, remaining)])
    } else {
      setImages((prev) => [...prev, ...valid])
    }
    // 重置 input 以便重复选择同一文件
    e.target.value = ''
  }

  const removeImage = (index: number) => {
    setImages((prev) => {
      URL.revokeObjectURL(prev[index].previewUrl)
      return prev.filter((_, i) => i !== index)
    })
  }

  const handleSubmit = async () => {
    if (!canSubmit) return
    setSubmitting(true)
    try {
      // 读取文件为 ArrayBuffer → IPC 传递
      const files = await Promise.all(
        images.map(async (img) => {
          const buf = await img.file.arrayBuffer()
          return {
            name: img.file.name,
            data: Array.from(new Uint8Array(buf)), // 序列化为 number[]
            mimeType: img.file.type || 'image/png',
          }
        }),
      )

      const result = await window.electronAPI.submitIssue({
        description: description.trim(),
        files,
      })

      if (result.success) {
        toast.success('问题已提交，感谢反馈！')
        resetForm()
        setOpen(false)
      } else {
        toast.error(result.error || '提交失败，请重试')
      }
    } catch (e) {
      toast.error(e instanceof Error ? e.message : '提交失败，请重试')
    } finally {
      setSubmitting(false)
    }
  }

  const descCount = description.length

  return (
    <DialogPrimitive.Root open={open} onOpenChange={handleOpenChange}>
      <DialogPrimitive.Portal>
        <DialogPrimitive.Overlay className="fixed inset-0 z-[100] bg-black/20 titlebar-no-drag" />
        <DialogPrimitive.Content className="fixed left-[50%] top-[50%] z-[100] translate-x-[-50%] translate-y-[-50%] w-[520px] max-h-[85vh] bg-dialog text-dialog-foreground shadow-2xl rounded-xl overflow-hidden titlebar-no-drag">
          {/* Header */}
          <div className="flex items-center justify-between px-5 h-12 border-b border-border/50">
            <h2 className="text-sm font-medium flex items-center gap-2">
              <Bug size={16} />
              问题反馈
            </h2>
            <button
              onClick={() => handleOpenChange(false)}
              className="rounded-md p-1.5 text-muted-foreground/60 hover:text-foreground hover:bg-muted"
            >
              <X size={16} />
            </button>
          </div>

          {/* Body */}
          <div className="px-5 py-4 space-y-4 overflow-y-auto max-h-[calc(85vh-48px)]">
            {/* 问题描述 */}
            <div className="space-y-1.5">
              <label className="text-sm font-medium">
                问题描述 <span className="text-red-400">*</span>
              </label>
              <textarea
                value={description}
                onChange={(e) => setDescription(e.target.value)}
                maxLength={MAX_DESC_LENGTH}
                placeholder="请描述遇到的问题（1-5000 字）"
                className="w-full h-28 resize-none rounded-md border border-border bg-background px-3 py-2 text-sm placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-primary"
              />
              <div
                className={`text-xs text-right ${
                  descCount > MAX_DESC_LENGTH * 0.9 ? 'text-red-400' : 'text-muted-foreground'
                }`}
              >
                {descCount}/{MAX_DESC_LENGTH}
              </div>
            </div>

            {/* 图片附件 */}
            <div className="space-y-2">
              <label className="text-sm font-medium">
                截图附件 <span className="text-muted-foreground font-normal">（可选，最多 {MAX_IMAGES} 张）</span>
              </label>

              {/* 图片列表 */}
              {images.length > 0 && (
                <div className="flex flex-wrap gap-2">
                  {images.map((img, i) => (
                    <div key={i} className="relative group w-20 h-20 rounded-md overflow-hidden border border-border">
                      <img
                        src={img.previewUrl}
                        alt={`截图 ${i + 1}`}
                        className="w-full h-full object-cover"
                      />
                      <button
                        onClick={() => removeImage(i)}
                        className="absolute top-0.5 right-0.5 w-5 h-5 rounded-full bg-black/60 text-white flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity"
                      >
                        <Trash2 size={12} />
                      </button>
                      <span className="absolute bottom-0 left-0 right-0 text-[10px] text-center bg-black/40 text-white py-0.5">
                        {i + 1}
                      </span>
                    </div>
                  ))}

                  {/* 添加按钮 */}
                  {images.length < MAX_IMAGES && (
                    <label className="w-20 h-20 rounded-md border-2 border-dashed border-border flex items-center justify-center cursor-pointer hover:border-primary/50 hover:bg-muted/30 transition-colors">
                      <Plus size={20} className="text-muted-foreground" />
                      <input
                        type="file"
                        multiple
                        accept="image/png,image/jpeg,image/gif,image/webp"
                        className="hidden"
                        onChange={handleFileChange}
                      />
                    </label>
                  )}
                </div>
              )}

              {/* 空状态：上传区域 */}
              {images.length === 0 && (
                <label className="flex flex-col items-center justify-center gap-1 h-24 rounded-md border-2 border-dashed border-border cursor-pointer hover:border-primary/50 hover:bg-muted/30 transition-colors">
                  <Plus size={20} className="text-muted-foreground" />
                  <span className="text-xs text-muted-foreground">添加截图</span>
                  <input
                    type="file"
                    multiple
                    accept="image/png,image/jpeg,image/gif,image/webp"
                    className="hidden"
                    onChange={handleFileChange}
                  />
                </label>
              )}
            </div>

            {/* 提交按钮 */}
            <Button
              onClick={handleSubmit}
              disabled={!canSubmit}
              className="w-full"
            >
              {submitting && <Loader2 size={14} className="animate-spin mr-1" />}
              {submitting ? '提交中...' : '提交反馈'}
            </Button>
          </div>
        </DialogPrimitive.Content>
      </DialogPrimitive.Portal>
    </DialogPrimitive.Root>
  )
}
```

### 5.8 渲染进程 — issue-report/index.ts（barrel）

```ts
// apps/electron/src/renderer/components/issue-report/index.ts

export { IssueReportDialog } from './IssueReportDialog'
export { IssueReportButton } from './IssueReportButton'
```

### 5.9 渲染进程 Atom

```ts
// apps/electron/src/renderer/atoms/issue-report.ts

import { atom } from 'jotai'

/** 问题反馈弹窗是否打开 */
export const issueReportOpenAtom = atom(false)
```

### 5.10 App.tsx 改动

```tsx
// 在现有 SettingsDialog import 附近新增：
import { IssueReportDialog } from '@/components/issue-report'

// 在 <SettingsDialog /> 附近新增：
<IssueReportDialog />
```

### 5.11 数据流总结

```
渲染进程 (IssueReportDialog)
  │  用户填写描述 + 选择图片
  │  客户端校验：描述非空/≤5000, 图片格式/大小/数量
  │  图片读取为 ArrayBuffer → 序列化为 number[]
  │
  ▼  window.electronAPI.submitIssue(input)
预加载 (preload/index.ts)
  │  ipcRenderer.invoke('issue:submit', input)
  ▼
主进程 (issue-report/index.ts → service.ts)
  │  → submitIssue(input)
  │    1. FormData: description + files(Blob) + clientVersion + clientPlatform
  │    2. httpUpload('/workmate/api/issues', { formData })
  │    （Cookie 由 hteip-client 自动注入）
  │
  ▼  HTTP POST /workmate/api/issues → 服务端
  │  服务端校验 + 存储 → { code, data }
  │
  ▼  解析响应 → IPC 返回
渲染进程
  │  toast.success / toast.error
  │  成功后关闭弹窗 + 清空表单 + 释放预览 URL
```

---

## 六、校验矩阵

| 位置 | 检查项 | 规则 |
|------|--------|------|
| 渲染进程 | 描述非空 | `description.trim().length > 0`，提交按钮联动禁用 |
| 渲染进程 | 描述长度 | `maxLength={5000}`，字符计数器标红提醒 |
| 渲染进程 | 图片格式 | `accept` + `ALLOWED_TYPES` + `ALLOWED_EXTENSIONS` 双重校验 |
| 渲染进程 | 图片大小 | `file.size <= 10 * 1024 * 1024`，选择时即过滤 |
| 渲染进程 | 图片数量 | 最多 9 张，达到上限后隐藏添加按钮 |
| 渲染进程 | 关闭确认 | 有未提交内容时 `window.confirm` 拦截 |
| 渲染进程 | 预览 URL 泄漏 | 删除 + 关闭时 `URL.revokeObjectURL` |
| 主进程 | FormData | 使用 `new Blob([new Uint8Array(data)])` 正确传递 binary |
| 主进程 | clientVersion | 使用 `app.getVersion()` |
| 主进程 | clientPlatform | 映射 `win32→windows, darwin→mac, linux→linux` |
| 服务端 | 所有校验 | 独立校验（Magic Number、路径穿越等），客户端校验仅为提升 UX |

---

## 七、一期不做

| 项目 | 原因 |
|------|------|
| 图片压缩 | 复杂度较高，后续迭代 |
| 图片拖拽排序 | 通过删除+重新添加实现重排 |
| 问题列表/历史记录 | 属于管理台功能 |
| 草稿持久化 | 关闭弹窗时有 confirm 确认即可 |
| 离线暂存 | 需要 IndexedDB 等基础设施 |
