# 用户上报异常日志 — 前端设计方案

## 1. 概述

在设置 → 系统日志页面的右上角新增"日志上报"按钮。用户点击后，将本地日志文件（`main.log`、`renderer.log`）打包为 zip，通过 HTTP `POST` 上传至服务端。

**设计原则**：业务逻辑全部放在新增文件中，对现有代码做最小侵入性改动。

**后端接口**：[服务端-日志上报.md](file:///D:/workmate-server/docs/服务端/服务端-日志上报.md)

---

## 2. 后端接口摘要

### 2.1 上传接口

```
POST /workmate/logs/upload
Content-Type: multipart/form-data
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `file` | MultipartFile | 是 | 日志压缩文件（仅 `.zip`、`.tar.gz`、`.tgz`） |
| `userId` | String | 否 | 用户标识（若网关请求头中已有则可不传） |

**服务端校验要点**：
- 文件非空、扩展名白名单（`.zip` / `.tar.gz` / `.tgz`）、大小上限 50MB
- 原始文件名不得含路径穿越字符（`/`、`\`、`..`）
- **jobId** 不得含 `/`、`\`、`..`、`:`、`*`、`?`、`"`、`<`、`>`、`|`
- **Zip 炸弹防御**：解压后总大小 ≤ 500MB，条目路径穿越检查
- **文件名由服务端生成**：`{jobId}_{yyyy-MM-dd-HH-mm}_{8位uuid}.zip`

**成功响应**（200）：

```json
{
  "code": 0,
  "message": "上传成功",
  "data": {
    "fileName": "022480_2026-06-10-12-23_a1b2c3d4.zip",
    "fileSize": 1048576,
    "filePath": "/app/workmate/user_log/022480_2026-06-10-12-23_a1b2c3d4.zip",
    "uploadedAt": "2026-06-08T14:30:22"
  }
}
```

**错误响应**：

```json
{ "code": 400, "message": "不支持的文件类型，仅允许 zip/tar.gz" }
```

### 2.2 鉴权

该接口挂载在 `/workmate/console/` 下，走 `AdminWhitelistInterceptor` 白名单鉴权。`jobId` 由网关从请求头注入（Header: `X-Workmate-UserId`），客户端在 multipart 表单中可附加 `userId` 字段。

> **前端处理**：请求头由网关/代理层自动注入，前端只需在表单中附带 `userId` 字段即可。

---

## 3. 前端改动清单

### 3.1 新增文件（4 个）

| 文件 | 职责 |
|------|------|
| `apps/electron/src/main/lib/log-uploader.ts` | 核心：打包 zip + HTTP 上传 |
| `apps/electron/src/main/lib/log-upload-ipc.ts` | 注册 `system-log:upload` IPC handler |
| `apps/electron/src/renderer/components/settings/LogUploadButton.tsx` | 按钮组件（确认框、loading、结果反馈） |
| `apps/electron/src/main/lib/log-upload-config.ts`（可选） | 上传接口地址等配置常量 |

### 3.2 修改现有文件（4 个，各 ≤ 5 行）

| 文件 | 改动 |
|------|------|
| `apps/electron/src/shared/hteip-client.ts` | 新增 `httpUpload` 方法（~90 行，完全内聚，不影响现有方法） |
| `apps/electron/src/main/index.ts` | +1 import + +1 行 `registerLogUploadIpc()` |
| `apps/electron/src/preload/index.ts` | 接口加 1 个方法签名 + 实现加 3 行 |
| `apps/electron/src/renderer/components/settings/SystemLogSettings.tsx` | +1 import + 加 1 行 `<LogUploadButton />` |

---

## 4. 交互流程

```
用户点击"日志上报"
  → window.confirm 确认框："将打包并上报以下日志文件…"
  → 用户确认
  → 渲染进程调用 window.electronAPI.uploadSystemLog()
  → 主进程 IPC handler：
    1. 从 UserProfile.userName 获取 jobId
    2. 校验 jobId 合法（不得含 /\..:*?"<>|）
    3. 读取 app.getPath('logs') 下的 main.log / renderer.log
    4. 用 adm-zip 在内存中打包为 zip
    5. 构建 FormData：file(zip buffer) + userId(jobId)
    6. POST /workmate/logs/upload
    7. 解析响应 → 返回 { success } 或 { success, fileName } 或 { success=false, error }
  → UI 弹出 toast 反馈
```

---

## 5. 新增文件详细设计

### 5.1 HTTP 客户端：`hteip-client.ts` 新增 `httpUpload`

在现有 [hteip-client.ts](file:///d:/AiWorkbench-workmate/apps/electron/src/shared/hteip-client.ts) 新增 `httpUpload` 方法，同时**将 URL 域名解析收敛到客户端内部**。

**核心改动：调用方不再关心域名**

```ts
// 之前：每个调用方都手动 buildUploadUrl()
const base = getEipGatewayBase()
const gatewayHost = base.replace(/\/gateway\/?$/, '')
const url = `${gatewayHost}/workmate/logs/upload`

// 现在：只传路径，域名由 hteip-client 内部 resolveUrl() 自动拼接
const res = await httpUpload<...>('/workmate/logs/upload', { formData })
```

`resolveUrl()` 规则：

| 参数 | 行为 |
|------|------|
| `/workmate/models` | 自动拼接 → `https://eip.workmate.com/workmate/models` |
| `http://localhost:6173/api` | 完整 URL，原样透传（开发调试用） |

**`httpUpload` 能力一览**：

| 方法 | Content-Type | body 处理 | 默认超时 | URL 解析 |
|------|-------------|-----------|---------|---------|
| `httpGet` | `application/json` | 无 | 10s | `resolveUrl()` |
| `httpPost` | `application/json` | `JSON.stringify` | 10s | `resolveUrl()` |
| **`httpUpload`** | **不设置（浏览器自动 `multipart/form-data; boundary=...`）** | **直接传 `FormData`** | **30s** | `resolveUrl()` |

- 复用 `buildAuthCookie()` → 自动注入 `Cookie: EIPGW-TOKEN=<jwt>`
- 复用 `combineSignals()` → 支持外部 AbortSignal + 内置超时并发触发
- 复用 `HttpResponse<T>` → 返回格式与 `httpGet`/`httpPost` 完全一致
- 默认超时 30s（文件上传比普通 API 更长）

### 5.2 `apps/electron/src/main/lib/log-uploader.ts`

```ts
/**
 * 日志上报模块
 *
 * 将本地 main.log / renderer.log 打包为 zip，通过 httpUpload
 * 上传到服务端 POST /workmate/logs/upload。
 *
 * URL 域名由 hteip-client 内部自动拼接，调用方只需传路径。
 */

import AdmZip from 'adm-zip'
import { join } from 'node:path'
import { existsSync, readFileSync } from 'node:fs'
import { app } from 'electron'
import { getUserProfile } from './user-profile-service'
import { httpUpload } from '../../shared/hteip-client'
import type { HttpResponse } from '../../shared/hteip-client'
import type { UserProfile } from '../../types'

// ── 常量 ──

/** 上传接口路径（域名由 hteip-client 内部拼接） */
const UPLOAD_PATH = '/workmate/logs/upload'

/** jobId 禁止字符（与后端保持一致） */
const JOB_ID_FORBIDDEN = '/\\:?*"<>|'

// ── 类型 ──

export interface LogUploadInput {
  /** 用户工号，为空时从 UserProfile.userName 兜底 */
  staffId?: string
}

interface LogUploadSuccess {
  success: true
  fileName: string
  fileSize?: number
  uploadedAt?: string
}

interface LogUploadError {
  success: false
  error: string
}

export type LogUploadResult = LogUploadSuccess | LogUploadError

/** IPC 通道名——硬编码，不修改 shared 包 */
export const LOG_UPLOAD_IPC_CHANNEL = 'system-log:upload'

// ── jobId ──

function resolveJobId(input: LogUploadInput): string {
  const fromInput = input.staffId?.trim()
  if (fromInput) return fromInput
  const profile: UserProfile = getUserProfile()
  return profile.userName?.trim() || 'unknown'
}

function validateJobId(jobId: string): void {
  if (!jobId || /\.\./.test(jobId)) {
    throw new Error('用户标识无效')
  }
  for (const ch of jobId) {
    if (JOB_ID_FORBIDDEN.indexOf(ch) >= 0) {
      throw new Error('用户标识包含非法字符')
    }
  }
}

// ── 打包 ──

function collectLogFiles(logsDir: string): Array<{ name: string; data: Buffer }> {
  const files: Array<{ name: string; data: Buffer }> = []
  for (const name of ['main.log', 'renderer.log']) {
    const filePath = join(logsDir, name)
    if (existsSync(filePath)) {
      files.push({ name, data: readFileSync(filePath) })
    }
  }
  if (files.length === 0) {
    throw new Error('没有可上报的日志文件')
  }
  return files
}

function buildClientFileName(jobId: string): string {
  const now = new Date()
  const pad = (n: number) => String(n).padStart(2, '0')
  const dateStr = [
    now.getFullYear(),
    pad(now.getMonth() + 1),
    pad(now.getDate()),
  ].join('-')
  const timeStr = [pad(now.getHours()), pad(now.getMinutes())].join('-')
  return `${jobId}_${dateStr}-${timeStr}_log.zip`
}

// ── 上传 ──

interface UploadApiResponse {
  code: number
  message: string
  data?: { fileName: string; fileSize: number; uploadedAt: string }
}

async function httpUploadZip(
  zipBuffer: Buffer,
  clientFileName: string,
  jobId: string,
): Promise<LogUploadResult> {
  const formData = new FormData()
  formData.append('file', new Blob([zipBuffer], { type: 'application/zip' }), clientFileName)
  formData.append('userId', jobId)

  console.log('[日志上报]  POST %s', UPLOAD_PATH)

  const res: HttpResponse<UploadApiResponse> = await httpUpload<UploadApiResponse>(
    UPLOAD_PATH,  // hteip-client 内部自动拼接为 https://eip.xxx/workmate/logs/upload
    { formData },
  )

  if (res.ok && res.data?.code === 0 && res.data.data) {
    return {
      success: true,
      fileName: res.data.data.fileName,
      fileSize: res.data.data.fileSize,
      uploadedAt: res.data.data.uploadedAt,
    }
  }

  const errorMsg = res.error || res.data?.message || `上传失败 (HTTP ${res.status})`
  return { success: false, error: errorMsg }
}

// ── 入口 ──

/**
 * 将本地日志打包为 zip 并上传到服务端。
 */
export async function uploadSystemLog(
  input: LogUploadInput,
): Promise<LogUploadResult> {
  const jobId = resolveJobId(input)
  validateJobId(jobId)

  const logsDir = app.getPath('logs')
  const logFiles = collectLogFiles(logsDir)

  const zip = new AdmZip()
  for (const { name, data } of logFiles) {
    zip.addFile(name, data)
  }
  const zipBuffer = zip.toBuffer()

  const clientFileName = buildClientFileName(jobId)

  console.log(
    `[日志上报] 已打包准备上传: ${clientFileName} (${(zipBuffer.byteLength / 1024).toFixed(1)} KB)`,
  )

  return httpUploadZip(zipBuffer, clientFileName, jobId)
}
```

### 5.3 `apps/electron/src/main/lib/log-upload-ipc.ts`

```ts
/**
 * 日志上报 IPC handler 注册
 *
 * 在 index.ts 初始化阶段调用 registerLogUploadIpc() 即可。
 */

import { ipcMain } from 'electron'
import { uploadSystemLog, LOG_UPLOAD_IPC_CHANNEL } from './log-uploader'
import type { LogUploadInput } from './log-uploader'

export function registerLogUploadIpc(): void {
  ipcMain.handle(
    LOG_UPLOAD_IPC_CHANNEL,
    async (_event, input: LogUploadInput | undefined) => {
      try {
        return await uploadSystemLog(input ?? {})
      } catch (err) {
        const message =
          err instanceof Error ? err.message : '日志上报失败'
        console.error('[IPC] system-log:upload 失败:', message)
        return { success: false, error: message }
      }
    },
  )
}
```

### 5.4 `apps/electron/src/renderer/components/settings/LogUploadButton.tsx`

```tsx
/**
 * 日志上报按钮
 *
 * 提供确认对话框 → 调用 electronAPI.uploadSystemLog() → toast 反馈
 */

import * as React from 'react'
import { Upload, Loader2 } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { toast } from 'sonner'

export function LogUploadButton(): React.ReactElement {
  const [uploading, setUploading] = React.useState(false)

  const handleUpload = React.useCallback(async () => {
    const confirmed = window.confirm(
      [
        '将打包并上报以下日志文件：',
        '• main.log（主进程）',
        '• renderer.log（页面进程）',
        '',
        '是否继续？',
      ].join('\n'),
    )
    if (!confirmed) return

    setUploading(true)
    try {
      const result = await window.electronAPI.uploadSystemLog()
      if (result.success) {
        toast.success(
          `日志已上报：${result.fileName ?? ''}`,
          { duration: 5000 },
        )
      } else {
        toast.error(`上报失败：${result.error ?? '未知错误'}`, { duration: 5000 })
      }
    } catch (e) {
      toast.error(
        `上报失败：${e instanceof Error ? e.message : '未知错误'}`,
        { duration: 5000 },
      )
    } finally {
      setUploading(false)
    }
  }, [])

  return (
    <Button
      variant="outline"
      size="sm"
      onClick={handleUpload}
      disabled={uploading}
    >
      {uploading ? (
        <Loader2 size={14} className="animate-spin" />
      ) : (
        <Upload size={14} />
      )}
      {uploading ? '上报中...' : '日志上报'}
    </Button>
  )
}
```

> tost 来自 `sonner`，项目中已有使用。

---

## 6. 现有文件改动（最小化）

### 6.1 `apps/electron/src/main/index.ts`

**import 区**（与其他 `./lib/` 导入放一起）：

```ts
import { registerLogUploadIpc } from './lib/log-upload-ipc'
```

**初始化阶段**（`registerIpcHandlers()` 附近）：

```ts
registerIpcHandlers()
registerLogUploadIpc()   // ← 新增
registerAuthIpcHandlers()
```

### 6.2 `apps/electron/src/preload/index.ts`

**接口定义处**（`ElectronAPI` interface 内）：

```ts
/** 上报系统日志到服务端 */
uploadSystemLog: () => Promise<
  | { success: true; fileName: string; fileSize?: number; uploadedAt?: string }
  | { success: false; error: string }
>
```

**实现处**（`electronAPI` 对象内）：

```ts
uploadSystemLog: () => {
  return ipcRenderer.invoke('system-log:upload')
},
```

### 6.3 `apps/electron/src/renderer/components/settings/SystemLogSettings.tsx`

**import 区**：

```tsx
import { LogUploadButton } from './LogUploadButton'
```

**按钮区域**（"打开目录" + "刷新" 所在 `<div>` 内）追加一行：

```tsx
<LogUploadButton />
```

---

## 7. 工号来源与校验

| 项目 | 说明 |
|------|------|
| **来源** | `UserProfile.userName`（可在通用设置中修改） |
| **参数预留** | `LogUploadInput.staffId` 可显式传入，为空时回退到 `userName` |
| **客户端校验** | 禁止 `/`、`\`、`..`、`:`、`*`、`?`、`"`、`<`、`>`、`|`（与后端一致） |
| **服务端校验** | 同上，双重保障 |
| **后续扩展** | 如需独立工号字段，在 `UserProfile` 中新增 `staffId` 并改 `resolveJobId()` 读取逻辑即可 |

---

## 8. 文件命名策略

| 阶段 | 谁生成 | 用途 | 格式 |
|------|--------|------|------|
| FormData 中的文件名 | 客户端 | HTTP multipart 的 `filename` 字段（仅标识，不被服务端采纳） | `{jobId}_{yyyy-MM-dd-HH-mm}_log.zip` |
| 最终存储的文件名 | 服务端 | 实际落盘文件名 | `{jobId}_{yyyy-MM-dd-HH-mm}_{8位uuid}.zip` |

---

## 9. 错误处理矩阵

| 环节 | 异常场景 | 用户看到 |
|------|----------|----------|
| jobId 校验 | `userName` 为空或含非法字符 | toast: "用户标识无效" |
| 文件读取 | `main.log` / `renderer.log` 均不存在 | toast: "没有可上报的日志文件" |
| zip 打包 | adm-zip 异常 | toast: "日志上报失败" |
| HTTP 请求 | 网络不通 / 超时（30s） | toast: "上报失败：请求超时" |
| 服务端拒绝 | 文件超 50MB | toast: "上报失败：文件大小超过限制（最大 50MB）" |
| 服务端拒绝 | 扩展名不合法 | toast: "上报失败：不支持的文件类型，仅允许 zip/tar.gz" |
| 服务端拒绝 | zip 炸弹 | toast: "上报失败：压缩包解压后大小超过限制（最大 500MB）" |
| 服务端拒绝 | 路径穿越 | toast: "上报失败：压缩包内文件路径非法" |
| 服务端异常 | HTTP 500 | toast: "上报失败：日志文件上传失败" |

---

## 10. 安全设计

| 层面 | 措施 |
|------|------|
| **客户端** | jobId 客户端预校验（禁止路径穿越字符），与服务端规则一致 |
| **传输** | `multipart/form-data`，文件名仅作为表单元数据，不被服务端采纳 |
| **服务端** | 原始文件名仅取扩展名，jobId 二次校验，zip 炸弹 + 路径穿越强制防御 |
| **隐私** | 上传前 pop 确认框，明确列出将上传的文件 |
| **不落盘** | zip 在内存中生成（`zip.toBuffer()`），不写客户端临时文件 |
| **依赖** | `adm-zip` 已是项目依赖，无需额外安装 |

---

## 11. 测试要点

### 11.1 `log-uploader.ts` 单元测试

| 用例 | 输入 | 预期 |
|------|------|------|
| 正常上传 | 有效的 `userName` + 存在log文件 | `{ success: true, fileName }` |
| jobId 含 `..` | `staffId: "test..abc"` | 抛出 "用户标识无效" |
| jobId 含 `/` | `staffId: "test/abc"` | 抛出 "用户标识包含非法字符" |
| 无日志文件 | logs 目录无 main.log/renderer.log | 抛出 "没有可上报的日志文件" |

### 11.2 `LogUploadButton.tsx` 组件测试

| 用例 | 预期 |
|------|------|
| 初始渲染 | 按钮文字"日志上报"，disabled=false |
| 点击 → 取消确认 | 不发起上传 |
| 点击 → 确认 | loading 态，按钮 disabled，"上报中..." |
| 上传成功 | toast.success + 按钮恢复 |
| 上传失败 | toast.error + 按钮恢复 |

---

## 12. URL 构建与环境

日志上报接口地址由 `hteip-client` 内部的 `resolveUrl()` 自动拼接，调用方只需传路径 `/workmate/logs/upload`，无需关心域名。

```
log-uploader.ts:  UPLOAD_PATH = '/workmate/logs/upload'
                      ↓
hteip-client.ts:  resolveUrl(path)
                      ↓ getEipGatewayBase().replace(/\/gateway\/?$/, '')
                      ↓
最终请求 URL:     https://eip.workmate.com/workmate/logs/upload
```

| 环境 | `eipGatewayBase`（settings.json） | 最终上传 URL |
|------|-------------------|--------------|
| 生产 | `https://eip.workmate.com/gateway` | `https://eip.workmate.com/workmate/logs/upload` |
| 测试 | `https://eip-test.workmate.com/gateway` | `https://eip-test.workmate.com/workmate/logs/upload` |
| 本地 | `http://localhost:8080/gateway` | `http://localhost:8080/workmate/logs/upload` |

上传超时固定 30 秒，由 `httpUpload` 内置 `AbortSignal.timeout()` 控制。
