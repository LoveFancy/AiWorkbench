# 专家团下载进度条与可取消方案

编辑时间：2026年6月22日

***

## 一、背景与问题

Agent 技能「专家」tab 页中，从服务端下载专家团插件包时存在两类体验问题：

1. **整个进程卡死**：专家包较大时，下载完成后的安装阶段会冻结整个应用 UI（无法点击、无法切换）。
2. **无进度反馈、无法取消**：下载耗时较长或专家包较大时，用户看不到任何进度，也无法中途取消。

### 三个根因

| 根因 | 位置 | 说明 |
| --- | --- | --- |
| **同步解压/拷贝阻塞主进程** | [`installUserPluginZip`](file:///d:/AiWorkbench-workmate/apps/electron/src/main/lib/plugin-registry-service.ts#L746) → [`extractPluginZipSafely`](file:///d:/AiWorkbench-workmate/apps/electron/src/main/lib/plugin-registry-service.ts#L659) | 逐条 `entry.getData()` + `writeFileSync` 同步写盘，再 `cpSync` 递归拷贝。大包长时间占用事件循环 → 应用冻结。**这是真正卡死的元凶**，下载本身是 `fetch` 流式非阻塞。 |
| **渲染端未消费进度** | 全渲染层 | 主进程已通过 `DOWNLOAD_PROGRESS` 广播 [`RemoteDownloadProgress`](file:///d:/AiWorkbench-workmate/packages/shared/src/types/agent.ts)，但**没有任何组件订阅**，用户看不到进度。 |
| **取消为空实现** | [`CANCEL_DOWNLOAD` handler](file:///d:/AiWorkbench-workmate/apps/electron/src/main/ipc.ts#L2511) | 仅打印日志，不真正中断。 |

### 现有可复用基建

- 进度类型 `RemoteDownloadProgress`、IPC 通道 `DOWNLOAD_REMOTE_EXPERT` / `DOWNLOAD_PROGRESS` / `CANCEL_DOWNLOAD` / `ENSURE_LATEST` 已存在。
- preload 已暴露 `downloadRemoteExpert` / `cancelRemoteDownload` / `onExpertDownloadProgress` / `ensureExpertGroupLatest`。
- 状态枚举 `remote_downloading` / `remote_download_failed` 已存在于 `AgentExpertGroupStatus`。
- 统一召唤入口 [`useSummonExpert`](file:///d:/AiWorkbench-workmate/apps/electron/src/renderer/experts/hooks/useSummonExpert.ts)、卡片 [`ExpertCard`](file:///d:/AiWorkbench-workmate/apps/electron/src/renderer/experts/card/ExpertCard.tsx) 已存在。

## 二、目标与非目标

### 目标

1. 安装/解压阶段**不再冻结主进程**，应用全程可交互。
2. 卡片**内联**展示下载进度（百分比 + 进度条），三个触发场景复用同一套 UI。
3. 下载 + 安装**全程可取消**，取消后清理临时文件、回退状态。
4. 下载完成后**自动打开会话的现有行为保持不变**。
5. 召唤时若检测到需要下载（未下载 / 版本升级），同样走进度条与可取消。

### 非目标

- 断点续传。
- 多专家并行下载的全局下载管理器。
- worker_threads / child_process 解压（见方案选型，本期用异步分片解压即可）。

## 三、设计选型

「不卡死」的关键是让解压/拷贝不阻塞事件循环。三种方案对比：

| 方案 | 做法 | 取舍 |
| --- | --- | --- |
| **A 异步分片解压（采用）** | `extractPluginZipSafely` 改 `async`，逐条 `fs.promises.writeFile` 并在条间 `await` 让出事件循环；`cpSync` 换 `fs.promises.cp`；条间检查 abort 标志 | 改动小、零打包风险、天然支持安装期取消。专家包多为大量小文件，逐条让出足以保持 UI 响应 |
| B worker_threads | 解压+拷贝整体丢子线程 | 即使超大文件也不阻塞，但需单独打包 worker、桥接 cancel/progress 复杂，对当前包体属过度工程 |
| C child_process unzip | 调系统 unzip | 完全不阻塞，但 Windows 无内置 unzip、丢失路径穿越安全校验，否决 |

**结论：采用方案 A。**

## 四、整体架构

```
主进程                          IPC                      渲染进程
─────────────────────────────────────────────────────────────────────
expert-download-service
  ├ AbortController 注册表    ◄──CANCEL_DOWNLOAD────  ExpertCard 取消按钮
  │  (per groupId)
  ├ fetch(signal) 流式下载    ───DOWNLOAD_PROGRESS──► 全局订阅 hook
  └ installUserPluginZipAsync                          │ 写入
     (异步分片解压 + await cp,                          ▼
      条间检查 abort)                            expertDownloadProgressAtom
                                                  Map<groupId, Progress>
                                                       │ 派生切片
                                                       ▼
                                                 ExpertCard 内联进度条
```

**三个触发场景统一走这套进度 + 取消：**

1. 专家 tab 页点「下载」→ `downloadRemoteExpert`
2. 召唤未下载专家 → `useSummonExpert` 分支一（下载后自动开会话，行为不变）
3. 召唤已下载专家的版本检测升级 → `ensureExpertGroupLatest` 内部复用 `downloadAndInstallRemoteExpert`，进度自动广播

## 五、主进程：去阻塞 + 真正可取消

### 5.1 异步分片解压（消除卡死）

在 [`plugin-registry-service.ts`](file:///d:/AiWorkbench-workmate/apps/electron/src/main/lib/plugin-registry-service.ts) 中，`extractPluginZipAsync` 和 `installUserPluginZipAsync` 已落地，但内部仍调用同步 `copyPluginAtomically` 和 `assertNoDuplicateExpertGroups`（见 §5.4 / §5.5）。本节描述其异步解压核心逻辑（已实现，无需改动），不改动现有同步 `installUserPluginZip`（仍供本地 zip 上传等场景使用）：

```ts
interface InstallAsyncOptions extends InstallUserPluginZipOptions {
  signal?: AbortSignal               // 安装期可取消
}

// extractPluginZipSafely 的异步版：逐条 await writeFile，条间 yield + 检查 abort
async function extractPluginZipAsync(zipPath, extractDir, signal?): Promise<void> {
  const zip = new AdmZip(zipPath)
  for (const entry of zip.getEntries()) {
    if (signal?.aborted) throw new DownloadCancelledError()
    // ...沿用现有路径穿越安全校验...
    if (entry.isDirectory) { await fsp.mkdir(targetPath, { recursive: true }); continue }
    await fsp.mkdir(dirname(targetPath), { recursive: true })
    await fsp.writeFile(targetPath, entry.getData())   // 条间让出事件循环
  }
}

export async function installUserPluginZipAsync(zipPath, options): Promise<AgentPluginInfo> {
  // 结构同步版完全一致，仅把 extract 换异步、cpSync → await fsp.cp、关键节点检查 signal
}
```

> `entry.getData()` 单条仍是同步 CPU，但专家包是大量小文件（md/json），逐条 `await` 已足够让 UI 保持响应——符合方案 A 的判断。

### 5.2 取消注册表 + 贯穿下载与安装

[`expert-download-service.ts`](file:///d:/AiWorkbench-workmate/apps/electron/src/main/lib/expert-download-service.ts) 改造：

```ts
// per-groupId 的 AbortController 注册表
const activeDownloads = new Map<string, AbortController>()

export function cancelRemoteDownload(groupId: string): void {
  activeDownloads.get(groupId)?.abort()
}

export async function downloadAndInstallRemoteExpert(groupId, options) {
  const controller = new AbortController()
  activeDownloads.set(groupId, controller)
  try {
    // 下载：fetch 的 signal 用 controller.signal（连接超时仍保留，二者合并）
    await downloadFile(url, tempPath, onProgress, controller.signal)
    broadcastProgress({ status: 'installing', ... })
    // 安装：传 signal，安装期也能取消
    const plugin = await installUserPluginZipAsync(tempPath, { ..., signal: controller.signal })
    broadcastProgress({ status: 'done', ... })
    return plugin
  } catch (err) {
    if (controller.signal.aborted) {
      broadcastProgress({ status: 'cancelled', ... })   // 新增状态
      throw new DownloadCancelledError()
    }
    broadcastProgress({ status: 'error', error, ... })
    throw err
  } finally {
    try { unlinkSync(tempPath) } catch {}          // 取消/失败都清理临时 zip
    activeDownloads.delete(groupId)
  }
}
```

- `downloadFile` 增加 `signal` 参，与现有 15s 连接超时的 controller 合并（`AbortSignal.any` 或共用同一 controller）。
- 取消时已解压的半成品目录：`installUserPluginZipAsync` 内部捕获 abort 后清理临时 `extractDir` 与 `.installing-*` 目录（复用现有 [`copyPluginAtomically`](file:///d:/AiWorkbench-workmate/apps/electron/src/main/lib/plugin-registry-service.ts#L700) 的失败清理逻辑）。

### 5.3 IPC + 类型

- [`CANCEL_DOWNLOAD` handler](file:///d:/AiWorkbench-workmate/apps/electron/src/main/ipc.ts#L2511)：从空实现改为调用 `cancelRemoteDownload(groupId)`。
- `RemoteDownloadProgress.status` 增加 `'cancelled'` 取值（[`agent.ts`](file:///d:/AiWorkbench-workmate/packages/shared/src/types/agent.ts)）。
- `ensureExpertGroupLatest` 不改签名——内部调用 `downloadAndInstallRemoteExpert`，自动获得进度广播与可取消能力。

### 5.4 单趟解压消除拷贝（2026-06-25 增补，取代原异步拷贝方案）

> 本节取代旧的「异步原子拷贝」设计。旧方案是**两趟全量写盘**：先解压到系统 temp，再 `fsp.cp` 把全部文件递归拷贝到安装区，最后 rename。对「1.5w+ 小文件」的大专家团，第二趟拷贝相当于把 1.5w 文件再写一遍盘——这是安装慢的主因（下载快、安装慢即源于此）。

**新方案：单趟解压。** 直接把 zip 解压进安装区**同盘**的 staging 目录，收尾只做一次原子 `rename`，彻底删除第二趟 `fsp.cp`。`copyPluginAtomicallyAsync` 在专家下载路径不再使用。

[`installUserPluginZipAsync`](file:///d:/AiWorkbench/apps/electron/src/main/lib/plugin-registry-service.ts) 重排为：

1. **先只读 manifest**：用 `AdmZip` 定位 `.claude-plugin/plugin.json`（根或唯一包裹子目录），单文件 `entry.getData()` 读出，算出 `installSlug` / `targetDir`，并做 `targetRel` 穿越校验。
2. **解压进安装区同盘 staging**：`staging = dirname(targetDir)/.<basename(targetDir)>.installing-<ts>`，`extractPluginZipAsync(zipPath, staging, signal, onProgress)`——**唯一一次** 1.5w 文件写盘。
3. **解析 pluginRoot**（staging 自身或其唯一子目录）→ `assertNoDuplicateExpertGroupsAsync`（在已解压的 staging 上跑，逻辑不变）。
4. **原子收尾**：overwrite 时先 `fsp.rm(targetDir)`，再 `fsp.rename(pluginRoot, targetDir)`（同盘瞬时）；写 config、返回 `pluginInfo`。
5. **`finally` 清理**：`fsp.rm(staging, { recursive: true, force: true })`，对取消/失败/已 rename 走的残留都安全。

**安全与正确性保持不变**：每个 entry 的路径穿越校验、`targetRel` 校验、`signal.aborted` 中止清理、专家团查重，全部保留。配 TDD 覆盖：包裹子目录场景、overwrite、中止清理、路径穿越拦截。

**解压真实进度**：`extractPluginZipAsync` 增加可选 `onProgress(processed, total)` 回调，循环内 `total = entries.length` 逐条上报，**按整数百分比节流**（最多 ~100 次回调，避免 1.5w 次 IPC 洪泛）。本地 zip 上传等其它调用方不传 `onProgress`，行为不变；单趟解压对它们是纯加速、无副作用。

### 5.5 异步全量扫描（消除安装期 + 安装后 listInstalledPlugins 阻塞）

两处同步全量扫描在安装期和安装后冻结主进程：

1. **安装期**：[`assertNoDuplicateExpertGroups`](file:///d:/AiWorkbench-workmate/apps/electron/src/main/lib/plugin-registry-service.ts#L826) 调用 `listInstalledPlugins`，逐个插件 `readdirSync` + `readFileSync` 扫描 skills/commands/agents/mcp/expert-group。
2. **安装后**：[`LIST_EXPERT_GROUPS` handler](file:///d:/AiWorkbench-workmate/apps/electron/src/main/ipc.ts#L2471) → [`listAgentExpertGroups`](file:///d:/AiWorkbench-workmate/apps/electron/src/main/lib/agent-expert-group-manager.ts#L207) → `listInstalledPlugins`，第二次全量扫描 + 每个专家团 `readExpertManifest(readFileSync)`。

新增异步扫描路径，与方案 A 解压思路一致——逐插件 `await` 让出事件循环，保持 UI 响应：

```ts
// 异步版：逐插件 await 让出事件循环
async function listInstalledPluginsAsync(paths?): Promise<AgentPluginInfo[]> {
  // 结构与同步版完全一致，仅把 readdirSync → await fsp.readdir、循环体内 await 让出
}

// 异步版查重
async function assertNoDuplicateExpertGroupsAsync(pluginPath, pluginId, manifest, paths, signal?): Promise<void> {
  const uploadedGroups = discoverExpertGroups(pluginPath, pluginId, manifest.name, true, manifest)
  if (uploadedGroups.length === 0) return
  const plugins = await listInstalledPluginsAsync(paths)
  // ...剩余逻辑同同步版...
}

// 异步版专家团列表，替换 LIST_EXPERT_GROUPS handler 的同步调用
async function listAgentExpertGroupsAsync(paths?): Promise<AgentExpertGroupInfo[]> {
  const plugins = await listInstalledPluginsAsync(paths)
  // ...剩余逻辑同同步版，仅 readExpertManifest 内部的 readFileSync 保留不变
  //   （单文件读取极快，不会成为瓶颈）
}
```

`installUserPluginZipAsync` 内部把 `assertNoDuplicateExpertGroups(...)` 替换为 `await assertNoDuplicateExpertGroupsAsync(...)`。`LIST_EXPERT_GROUPS` handler 改为 `await listAgentExpertGroupsAsync(paths)`。

> 逐插件 `await` 让出与解压同理：插件数通常 ≤ 20，逐条让出足以保持 UI 响应，无需 worker。

### 5.6 安装阶段真实进度广播（2026-06-25 增补）

原 [`downloadAndInstallRemoteExpert`](file:///d:/AiWorkbench/apps/electron/src/main/lib/expert-download-service.ts) 在安装前只广播**一次** `{ status:'installing', progress:100 }`，之后解压全程**无任何更新**——渲染端只能展示来回跑的不确定条带，1.5w 文件解压期间用户极易误判为「卡死」。

改为把 `onProgress` 透传给 `installUserPluginZipAsync` 并分阶段广播（仅在 `expert-download-service.ts` 内，属专家下载域职责）：

| 时机 | 广播 |
| --- | --- |
| 解压前 | `{ status:'installing', installStage:'extracting', progress:0, processedFiles:0, totalFiles }` |
| 解压中（节流，整数百分比变化才发） | `{ installStage:'extracting', progress: Math.round(processed/total*95), processedFiles, totalFiles }`（0→95） |
| 收尾（查重 + rename + 写 config） | `{ installStage:'finalizing', progress:95 }` |
| 完成 | `{ status:'done', progress:100 }` |

进度封顶 95% 给收尾留量，单趟方案下收尾极短，读作「即将完成」而非卡住。

### 6.1 进度 atom

在 [`expert-remote.ts`](file:///d:/AiWorkbench-workmate/apps/electron/src/renderer/experts/atoms/expert-remote.ts) 新增：

```ts
/** 各专家团下载进度 Map — groupId → RemoteDownloadProgress */
export const expertDownloadProgressAtom = atom<Map<string, RemoteDownloadProgress>>(new Map())

/** 按 groupId 切片订阅，避免任一进度更新触发所有卡片重渲染 */
export const expertDownloadProgressFamily = atomFamily((groupId: string) =>
  atom((get) => get(expertDownloadProgressAtom).get(groupId)),
)
```

### 6.2 全局订阅 hook（只挂一次）

新增 `experts/hooks/useExpertDownloadProgressBridge.ts`，在应用根组件 [`App.tsx`](file:///d:/AiWorkbench-workmate/apps/electron/src/renderer/App.tsx) 挂载一次（不是 ExpertPageView——否则切 tab 后 bridge 卸载，终态事件丢失，进度卡在 installing）：

```ts
useEffect(() => window.electronAPI.onExpertDownloadProgress((p) => {
  setProgressMap((prev) => new Map(prev).set(p.groupId, p))
  if (p.status === 'done' || p.status === 'cancelled' || p.status === 'error') {
    setTimeout(() => removeEntry(p.groupId), 1500)   // 终态短暂展示后清理
    if (p.status === 'done') void loadGroups()        // done 时刷新本地列表
  }
}), [])
```

> 进度订阅是全局单例桥接：即使切到别的 tab，下载仍在主进程后台进行，回来时进度照常显示。

### 6.3 卡片内联 UI（方案 A：底部状态区）

[`ExpertCard`](file:///d:/AiWorkbench-workmate/apps/electron/src/renderer/experts/card/ExpertCard.tsx) 订阅 `expertDownloadProgressFamily(group.id)`，把进度从右上角 `absolute`（与收藏星争位、视觉层级弱）移到**卡片底部独立状态区**（`border-top` 分隔），与召唤/下载按钮共用同一块底部空间，切换自然：

```
┌──────────────────────────────────┐
│ ★                                │
│ 🤖 产品专家团                      │
│ 主角色：产品经理                    │
│ 覆盖需求分析、PRD 撰写…            │
│ [产品] [需求] [PRD]               │
├──────────────────────────────────┤  ← border-top 分隔
│ ● 正在下载              63%   ✕   │  ← 状态行：状态点 + 文案 + 百分比 + 取消
│ ▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░    │  ← 确定态进度条（按 progress 填充）
└──────────────────────────────────┘
```

**完整状态机：**

| 状态 | 底部区域内容 |
| --- | --- |
| 无下载（默认） | 召唤/下载按钮（hover 显示，现有行为不变） |
| `downloading` | 蓝色状态点 +「正在下载」+ 百分比（`tabular-nums` 防抖动）+ **确定态进度条**（按 `progress` 填充）+ 取消 ✕ |
| `installing` / `extracting` | 紫色状态点 +「正在解压 {processedFiles}/{totalFiles}」+ 百分比 + **只前进的确定态进度条**（按真实 `progress` 0→95 填充）+ 取消 ✕ |
| `installing` / `finalizing` | 紫色状态点 +「正在校验并写入…」+ 进度条停在 ~95%（单趟方案下极短，读作「即将完成」）+ 取消 ✕ |
| `error` | 红色状态点 +「下载失败」+「重试」按钮（内联，1.5s 后由 bridge 清理回退默认态） |
| `cancelled` | 灰色状态点 +「已取消」+「下载」按钮（内联回退，1.5s 后由 bridge 清理） |

**细节说明：**

- 安装态改为**只前进的确定态进度条**（按真实 `progress` 填充），不再用来回跑的流动条纹——后者正是「看起来卡死」的根源。原 `download-progress-slide` keyframe 在 `globals.css` 中仅此一处引用，一并删除（无其它引用，见 §九）。
- **SRP / 行数控制**：把卡片底部下载/安装状态区从 [`ExpertCard.tsx`](file:///d:/AiWorkbench/apps/electron/src/renderer/experts/card/ExpertCard.tsx) 抽成独立组件 `card/ExpertDownloadStatus.tsx`（单一职责：渲染 downloading/extracting/finalizing/error/cancelled 五态），给 ExpertCard 瘦身；新组件经 `card/index.ts` 导出。
- 进度条仍沿用项目内手写样式（ui/ 下无 Progress 组件），宽度不写死，自适应底部区域。
- 取消按钮用 `✕` 图标，hover 时背景加深，与状态行在同一行右侧。
- `error` / `cancelled` 为终态，由 bridge 的 `setTimeout(removeEntry, 1500)` 自动清理；卡片只需在 `downloadProgress` 存在时渲染对应终态 UI。

### 6.4 三处触发点接入（逻辑几乎不动）

| 触发点 | 改动 |
| --- | --- |
| tab 页「下载」按钮 | 已调 `downloadRemoteExpert`，无需改；进度由订阅自动驱动 |
| 召唤未下载 [`useSummonExpert`](file:///d:/AiWorkbench-workmate/apps/electron/src/renderer/experts/hooks/useSummonExpert.ts) 分支一 | `await downloadRemoteExpert` 不变 → 完成后 `createExpertSession` + `openSession` **行为保持不变**；下载期卡片底部显示进度条 |
| 召唤版本检测升级 `ensureExpertGroupLatest` | 主进程内部已广播进度，卡片自动显示；召唤主流程降级逻辑不变 |

**关键保证**：「下载完成后自动打开会话」依赖 `downloadRemoteExpert` IPC promise 的 resolve 时机，进度订阅是旁路（side-channel），**不改变 await 链**，自动开会话行为完全不变。

## 七、错误处理与边界

| 场景 | 行为 |
| --- | --- |
| 下载期取消 | `fetch` abort → 清理临时 zip → 广播 `cancelled` → 卡片回退按钮 |
| 安装期取消 | 解压循环检测 abort 抛出 → 清理 `extractDir`/`.installing-*` → 广播 `cancelled` |
| 网络失败 / HTTP 非 200 | 广播 `error` → 卡片短暂提示后回退；召唤场景降级本地版（升级路径）或 toast 失败（未下载路径） |
| 召唤未下载时取消 | `downloadRemoteExpert` reject（`DownloadCancelledError`）→ `useSummonExpert` catch 中区分取消与失败：取消不弹 error toast，不开会话 |
| 切 tab 后回来 | 进度 Map 为全局 atom，下载在主进程后台继续，回来照常显示 |
| 同一专家重复点下载 | 主进程 `activeDownloads` 已有该 groupId 时直接复用/忽略，避免并发写 |

## 八、测试要点

- **主进程单测**：`extractPluginZipAsync` 正确解压 + 路径穿越拦截不变；`signal.aborted` 时抛 `DownloadCancelledError` 且清理临时目录；`cancelRemoteDownload` 能 abort 进行中的下载。
- **去阻塞验证**：下载安装大包期间主进程事件循环仍可响应（可用大体积 zip fixture 验证 install 期间定时器仍按时触发）。
- **召唤行为回归**：未下载专家召唤 → 下载完成后自动开会话；版本升级召唤 → 进度展示 + 失败降级本地版不阻断。

## 九、改动文件清单

| 文件 | 改动 |
| --- | --- |
| [`packages/shared/src/types/agent.ts`](file:///d:/AiWorkbench-workmate/packages/shared/src/types/agent.ts) | `RemoteDownloadProgress.status` 增 `'cancelled'` |
| [`plugin-registry-service.ts`](file:///d:/AiWorkbench-workmate/apps/electron/src/main/lib/plugin-registry-service.ts) | 新增 `extractPluginZipAsync` + `installUserPluginZipAsync`（含 signal）；新增 `copyPluginAtomicallyAsync`（`cpSync` → `fsp.cp`）；新增 `listInstalledPluginsAsync` + `assertNoDuplicateExpertGroupsAsync`（消除同步扫描阻塞） |
| [`agent-expert-group-manager.ts`](file:///d:/AiWorkbench-workmate/apps/electron/src/main/lib/agent-expert-group-manager.ts) | 新增 `listAgentExpertGroupsAsync`（消除安装后同步扫描阻塞） |
| [`expert-download-service.ts`](file:///d:/AiWorkbench-workmate/apps/electron/src/main/lib/expert-download-service.ts) | `activeDownloads` 注册表 + `cancelRemoteDownload` + `downloadFile` 加 signal + 走异步安装 |
| [`ipc.ts`](file:///d:/AiWorkbench-workmate/apps/electron/src/main/ipc.ts#L2511) | `CANCEL_DOWNLOAD` handler 改为真正取消；`LIST_EXPERT_GROUPS` handler 改为 `await listAgentExpertGroupsAsync` |
| [`expert-remote.ts`](file:///d:/AiWorkbench-workmate/apps/electron/src/renderer/experts/atoms/expert-remote.ts) | 新增 `expertDownloadProgressAtom` + family |
| `experts/hooks/useExpertDownloadProgressBridge.ts` | 新增全局订阅 hook |
| [`App.tsx`](file:///d:/AiWorkbench-workmate/apps/electron/src/renderer/App.tsx) | 挂载订阅 hook（根节点，确保切 tab 不丢失事件） |
| [`ExpertCard.tsx`](file:///d:/AiWorkbench-workmate/apps/electron/src/renderer/experts/card/ExpertCard.tsx) | 底部状态区内联进度 UI（完整状态机：下载/安装/失败/取消）+ 取消按钮 |
| [`useSummonExpert.ts`](file:///d:/AiWorkbench-workmate/apps/electron/src/renderer/experts/hooks/useSummonExpert.ts) | catch 区分取消/失败（不弹 error、不开会话） |

### 9.1 本次增补改动（2026-06-25，安装阶段真实进度）

> 上表为该特性整体清单（多数已实现）。本次仅做「安装阶段真实进度 + 单趟解压加速」增量，触及文件如下。约束：渲染端改动全部落在 `renderer/experts/` 内；主进程仅动专家下载域 `expert-download-service.ts` 与共享安装器 `plugin-registry-service.ts`（单趟解压是对所有调用方的纯加速）。

| 文件 | 本次改动 | 内聚 |
| --- | --- | --- |
| [`packages/shared/src/types/agent.ts`](file:///d:/AiWorkbench/packages/shared/src/types/agent.ts) | `RemoteDownloadProgress` 加 3 个可选字段 `installStage?` / `processedFiles?` / `totalFiles?`（进度契约，唯一不可避开的 shared 改动） | 契约 |
| [`plugin-registry-service.ts`](file:///d:/AiWorkbench/apps/electron/src/main/lib/plugin-registry-service.ts) | `installUserPluginZipAsync` 改单趟解压（删第二趟 `fsp.cp`）；`extractPluginZipAsync` 加节流 `onProgress`；`copyPluginAtomicallyAsync` 若无其它调用方则移除 | 共享安装器 |
| [`expert-download-service.ts`](file:///d:/AiWorkbench/apps/electron/src/main/lib/expert-download-service.ts) | 透传 `onProgress`，分阶段广播 extracting(0→95)/finalizing(95)/done(100) | 专家域 ✅ |
| [`ExpertCard.tsx`](file:///d:/AiWorkbench/apps/electron/src/renderer/experts/card/ExpertCard.tsx) | 移除底部状态区内联 UI（迁出）+ 移除流动条纹动画引用 | experts ✅ |
| `renderer/experts/card/ExpertDownloadStatus.tsx`（新） | 单一职责：渲染下载/解压/校验/失败/取消五态 + 取消按钮 | experts ✅ |
| [`card/index.ts`](file:///d:/AiWorkbench/apps/electron/src/renderer/experts/card/index.ts) | 导出新组件 | experts ✅ |
| [`globals.css`](file:///d:/AiWorkbench/apps/electron/src/renderer/styles/globals.css) | 删除已无引用的 `download-progress-slide` keyframe | 清理 |
