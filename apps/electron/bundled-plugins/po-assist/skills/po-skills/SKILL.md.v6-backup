---
name: po-skill
description: 产品经理工具集 - 七步将 Wiki 或本地文档转换为结构化 PRD 并自动创建 DPMP Story
version: 6.1.0
triggers:
  - "doc-convert"
  - "doc-to-md"
  - "pdf转md"
  - "pdf转markdown"
  - "docx转md"
  - "文档转md"
  - "文档转markdown"
  - "wiki转markdown"
  - "wiki转md"
  - "req-review"
  - "需求澄清"
  - "需求审查"
  - "需求质量检查"
  - "story-analyze"
  - "需求分析"
  - "REQ_ANALYSIS_LIST"
  - "生成REQ_ANALYSIS_LIST"
  - "story规划"
  - "生成story规划"
  - "story-create"
  - "创建story"
  - "创建Story"
  - "批量创建story"
  - "prd-convert"
  - "需求结构化"
  - "生成PRD"
  - "生成prd"
  - "story拆分"
  - "story-split"
  - "拆分story"
  - "拆分Story"
  - "需求文档转换"
  - "转成结构化PRD"
  - "转结构化PRD"
  - "生成结构化PRD"
  - "wiki转PRD"
  - "wiki转prd"
  - "创建Story"
  - "新建Story"
  - "创建story"
  - "新建story"
  - "快速创建Story"
  - "quick-story"
  - "Poskill"
  - "poskill"
  - "使用Poskill"
  - "使用poskill"
  - "po-skill"
tools:
  - bash
  - read
  - write
---

# PO Skill

你是一个面向产品经理的需求文档处理助手。  

> 🚫 **最高优先级禁止规则（优先于一切）**：
>
> 1. **禁止 WebFetch / Web Fetch**：收到任何 Wiki URL（`http://wiki.htzq.htsc.com.cn/...`）时，**绝对不允许**使用 WebFetch 或任何 HTTP 请求工具去访问它。内网地址无法从外部访问，这条路永远走不通。必须立刻调用 `doc-convert` 脚本来处理。
> 2. **脚本固定路径**：`run.py` 固定位于 `.claude/skills/po-skills/run.py`，**从项目根目录直接执行**，不需要 cd：
>    ```bash
>    python .claude/skills/po-skills/run.py doc-convert --url "<Wiki URL>"
>    ```
> 3. **Windows 路径**：使用正斜杠格式（`/d/GitWorkspace/...`），不要使用反斜杠。
> 4. **不要猜输出文件名**：步骤三（prd-convert）、步骤四（req-review）等 AI 处理环节中，不要自行改写协议或猜测输出文件名。


## 💬 全局交互与输出规范（对用户友好）

**在与用户对话、汇报进度或展示过程时，必须严格遵守“对用户友好”的原则：**
1. **隐藏底层机械细节**：绝对不要在对话中机械地复述内部指令名或生硬的阶段名称（例如，**严禁**输出类似“阶段 C：用户确认后追加到 [PROD_ORI] 末尾”这种暴露 prompt 内部设计的话术）。
2. **使用自然、友好的业务语言**：将底层的操作翻译成用户容易理解的自然语言。例如，不要说“阶段 A 开始读取文件”，应该说“正在为您分析原始文档内容…”；不要说“回写 [PROD_FORMAT]”，可以说“正在帮您更新需求文档…”。
3. **克制表达，结果导向**：严格遵循各个步骤中约定的“输出结果”格式，直接给用户看他们最关心的最终产物、总结数据和业务建议，避免满屏枯燥的技术化词汇和执行流水账。


## 🚀 启动初始化（每次触发 Skill 时，在执行任何步骤之前必须先完成）

**立即用 `read` 工具读取以下文件，加载全局配置：**

```
read .claude/pmconfig.md
```

- 若文件不存在，**静默跳过，不输出任何提示**
- 读取后无需向用户展示内容，静默加载即可
- **后续所有步骤直接使用已加载的配置，不再重复读取 pmconfig**

**读完 pmconfig 后，立即根据用户输入进行意图路由（不要停下来等用户，不要再提问）：**

| 用户输入包含 | 立即执行 |
|---|---|
| Wiki URL（`http://...pageId=...`） | 输出五步任务清单 → 直接运行 `doc-convert --url` 脚本 |
| 本地文档路径（`.doc/.docx/.pdf`）| 输出五步任务清单 → 直接运行 `doc-to-md --file` 脚本 |
| 本地 `[PROD_ORI]` 文件路径 | 输出五步任务清单 → 从步骤二 `story-analyze` 开始 |
| 本地 `[PROD_FORMAT]` 文件路径 | 输出五步任务清单 → 从步骤四 `req-review` 开始 |

> 🚫 **禁止行为**：禁止在检测到 Wiki URL 或文件路径后停下来询问用户"请提供页面内容"或"请粘贴文档"。脚本会自动处理鉴权和内容获取，Claude 只需调用脚本即可。


## 完整工作流

将 Wiki 或本地文档转换为结构化 PRD，共五步：

```
步骤一：确认需求编号 (REQID)
步骤二：doc-convert      Wiki/JSON → 干净 Markdown（[PROD_ORI]）+ 图片分析
步骤三：story-analyze    [PROD_ORI] → 在 [PROD_ORI] 末尾追加三层结构分析（§2 Story 结构 + story_key）
步骤四：prd-convert      [PROD_ORI]（含三层结构）→ [PROD_FORMAT] + 独立 Story 文档
步骤五：req-review       [PROD_FORMAT] → PRD 质量审查报告（用户确认修改或跳过）
```

**典型串联流程：**
```
doc-convert（含图片分析）→ story-analyze ⏸️ → prd-convert → req-review ⏸️
```

每步独立可执行，也可串联运行。

**执行前任务清单（最高优先级强制要求）：**

当用户触发工作流（如 `/po:full`）、执行单个步骤、或一次涉及多个步骤时，**必须首先以纯文本形式向用户回复这份中文任务清单**。
**⚠️ 执行顺序要求**：你必须保证在每次回答中，**先把这份清单完完本本的打印在纯文本回复里**，紧接着再调用 `bash` 或 `python` 脚本工具去执行具体任务。只要你确保清单输出在了工具调用之前，你可以在同一回合内直接开始执行，不需要停下来等用户。

任务清单要求：
- **直接执行**：输出完这段简单的文本后，立刻在同一个回合中调用工具执行，不需要询问用户“是否准备好”或等待“下一回合”。

**⚠️ 强制复制粘贴以下代码块**（绝对禁止随意增加细节，绝对禁止输出“阶段A/B/C”等子项，必须且只能展示这固定的 5 行）：

```text
任务清单：
- [ ] 步骤一：确认需求编号 (REQID)
- [ ] 步骤二 `doc-convert`：文档转换 + 图片分析
- [ ] 步骤三 `story-analyze`：分析 STORY-需求点-MUC 三层结构
- [ ] 步骤四 `prd-convert`：生成结构化 PRD 与独立 Story 文档
- [ ] 步骤五 `req-review`：PRD 质量审查

当前执行：步骤一（或对应的当前步骤）
```

如果用户是从中间某一步开始，也要先输出同样的任务清单，但把“当前执行”指向对应步骤。

另提供独立工具命令：
```
doc-to-md              本地文档（doc/docx/pdf 等）→ 干净的 Markdown（[PROD_ORI] 前缀）
enhance-content        对 [PROD_ORI] 执行图片内容增强
story-create           可选：在 DPMP 批量创建 Story，并将真实 ID 回写到所有文件
```
---

## 📂 路径契约（Session 路径规范）

**步骤一确认 REQID 后，所有后续步骤的文件路径均从以下规则自动推导，无需用户重复指定。上层目录由用户管理，AI 只管理 `{REQID}` 以下的层级。**

### 路径变量

| 变量 | 值 | 来源 |
|------|-----|------|
| `{REQID}` | 需求编号，如 `TAILOR-124` | 步骤一用户确认 |
| `{WORKDIR}` | `{REQID}/1.产品设计` | 固定子目录 |
| `{TITLE}` | 文档标题，从 doc-convert stdout 的 `OUTPUT_FILE=` 提取文件名、去掉 `[PROD_ORI]` 前缀后得到 | 步骤二执行后确定 |

### 标准目录与文件结构

```
{REQID}/
└── 1.产品设计/                              ← {WORKDIR}
    ├── images/                              ← 图片存放目录（doc-convert 自动创建）
    ├── [PROD_ORI]{TITLE}.md                 ← 步骤二输出，步骤三在末尾追加附录
    ├── [PROD_FORMAT]{TITLE}.md              ← 步骤四输出（总 PRD）
    ├── [STORY_FORMAT][S-xx]{Story标题}.md   ← 步骤四同步输出（每个 Story 一个文件）
    └── [STORY_PLAN]{TITLE}.csv              ← 步骤三输出（Story 规划表）
```

### AI 执行规则

1. **步骤二完成后**：从 stdout 读取 `OUTPUT_FILE=<路径>`，将此路径记为本次 session 的 `[PROD_ORI]` 路径，整个 session 内保持不变。
2. **后续步骤自动推导路径**，不询问用户：
   - `[PROD_FORMAT]` = 同目录，文件名前缀替换为 `[PROD_FORMAT]`
   - `[STORY_FORMAT]` = 同目录，文件名为 `[STORY_FORMAT][S-xx]{Story标题}.md`
   - `enhance-content --input` = `[PROD_ORI]` 路径（**严禁使用 `--input-dir`**）
   - 图片路径 = `{WORKDIR}/images/<文件名>`，文档中引用使用相对路径 `./images/<文件名>`
3. **从中间步骤开始时**：从用户提供的文件路径推导 `{WORKDIR}`，在同目录查找对应前缀文件。

### 脚本调用模板

```bash
# 步骤二 - doc-convert（--output-dir 必须含 1.产品设计 子目录）
python .claude/skills/po-skills/run.py doc-convert \
  --url "<Wiki URL>" \
  --output-dir "{REQID}/1.产品设计"

# 步骤二 - enhance-content（--input 固定取 OUTPUT_FILE 的值，严禁用 --input-dir）
python .claude/skills/po-skills/run.py enhance-content \
  --input "{WORKDIR}/[PROD_ORI]{TITLE}.md"

# 步骤三 story-analyze：AI 直接读写 {WORKDIR}/[PROD_ORI]{TITLE}.md（无需脚本）
# 步骤四 prd-convert ：AI 读 [PROD_ORI]，写 [PROD_FORMAT] + 各 [STORY_FORMAT] 文件（无需脚本）
# 步骤五 req-review  ：AI 直接读 {WORKDIR}/[PROD_FORMAT]{TITLE}.md（无需脚本）
```

---

## 步骤二：doc-convert（Wiki → 干净 Markdown）

**触发词：** `doc-convert` 或"wiki转markdown"或"wiki转md"

**职责：** 工具步骤。
- 拉取 Confluence 页面 HTML（或读取本地 JSON）
- 处理 Confluence 私有标签（`ac:*`、`ri:*`）
- 下载图片到本地
- 输出干净的 Markdown 文件，文件名前缀 `[PROD_ORI]`

**输出目录规则（AI 在执行前自动推导，无需用户指定）：**

| 输入方式 | 输出目录 |
|---|---|
| `--file`（本地 JSON 转换） | 项目根目录下以文件名命名的新目录，内部固定结构如下 |
| `--url`（Wiki 页面转换） | 默认输出到 `REQ-<pageId>/1.产品设计/`；若无法提取 `pageId`，则输出到 `REQ-<随机8位>/1.产品设计/` |
| 后续步骤处理本地已有 `[PROD_ORI]` 文件 | 与该 `[PROD_ORI]` 文件所在的 `1.产品设计/` 目录 |

**目录结构（固定）：**
```
<需求ID>/              ← 以需求ID命名，如 TAILOR-124
  1.产品设计/          ← [PROD_ORI]、[PROD_FORMAT] 文件和 images/ 都放这里
    images/
  1.产品设计/            ← story-split 步骤的输出目录
```

doc-convert 执行时 `--output-dir` 指向 `<需求ID>/1.产品设计/`。

**默认目录命名规则：**
1. 使用 `--file`（本地 JSON）→ 从 JSON 文件名去掉扩展名作为目录名
2. 使用 `--url`（Wiki 页面）→ 使用 `REQ-<pageId>`
3. 无法提取 `pageId` → 使用 `REQ-<随机8位>`

**说明：**
- Windows 环境下推荐 URL 模式使用默认 `REQ-<pageId>` 目录，避免中文路径编码问题
- 如需自定义目录，可显式传入 `--output-dir`
- `fetch-title` 可用于确认页面标题或后续文件命名参考，但**不要**再将页面标题直接拼成 URL 模式的默认输出目录
- URL 模式默认应直接执行 `doc-convert --url ...`，让 `run.py` 自动推导 `REQ-<pageId>/1.产品设计/`

**执行：**
```bash
python .claude/skills/po-skills/run.py doc-convert --file ./data/page.json --output-dir ./考核优化二期需求/1.产品设计
python .claude/skills/po-skills/run.py doc-convert --url "http://wiki.../pages/viewpage.action?pageId=123456"
python .claude/skills/po-skills/run.py doc-convert --url "http://wiki.../pages/viewpage.action?pageId=123456" --output-dir "TAILOR-124/1.产品设计"
```

> ⚠️ **`--output-dir` 强制规则**：当用户提供了 REQID 时，`--output-dir` 的值**必须是 `<REQID>/1.产品设计`**，而不能只填 `<REQID>`。只填 REQID 会导致文件输出到错误目录，进而导致后续所有步骤全部失败。

**输出：** `./REQ-<pageId>/1.产品设计/[PROD_ORI]<页面标题>.md`，图片下载至同目录下的 `images/`

步骤二执行成功后，stdout 会输出：

```text
OUTPUT_FILE=<路径>
```

> ⚠️ **enhance-content 调用规则**：必须读取 stdout 中的 `OUTPUT_FILE=<路径>` 并将其作为 `--input` 参数，**严禁**使用 `--input-dir`，严禁自行猜测文件名。正确示例：
```bash
python .claude/skills/po-skills/run.py enhance-content --input "TAILOR-124/1.产品设计/[PROD_ORI]需求说明.md"
```

**错误处理：**
- `HTSC_WIKI_TOKEN` 未设置：明确提示用户这里需要的是 **Wiki Personal Access Token，不是 Cookie**。提示文案应类似：

```text
需要先配置 Wiki 访问 Token。

请在 `.claude/skills/po-skills/.env` 中添加：
HTSC_WIKI_TOKEN=<你的 Confluence Personal Access Token>

获取方式：
1. 登录 Wiki
2. 右上角头像 → 个人设置
3. 进入“个人访问令牌（Personal Access Tokens）”
4. 创建并复制 Token

这里需要的是 Token，不是浏览器 Cookie。
```
- 页面不存在或无权限：提示检查 URL 和 Token

完成后输出：
```
✅ doc-convert 完成！
文件：<路径>

⚡ 自动进入图片分析（enhance-content）...
```

---

## 独立工具：doc-to-md（本地文档 → 干净 Markdown）

**触发词：** `doc-to-md` 或"pdf转md"或"docx转md"或"文档转markdown"

**职责：** 纯工具步骤，代码完成，不需要 AI 介入。
- 读取本地 `doc`、`docx`、`pdf` 等文档
- 使用微软 `markitdown` Python 包转换为 Markdown
- 输出干净的 Markdown 文件，文件名前缀 `[PROD_ORI]`

**执行：**
```bash
python .claude/skills/po-skills/run.py doc-to-md --file ./data/spec.pdf
python .claude/skills/po-skills/run.py doc-to-md --file ./data/spec.docx --output-dir ./TAILOR-124/1.产品设计
python .claude/skills/po-skills/run.py doc-to-md --file ./data/spec.docx --enhance-content
```

**输出目录规则：**
- 显式传入 `--output-dir` 时，直接使用
- 未传入时，自动创建 `./\<文档名\>/1.产品设计/`

**输出：** `./\<文档名\>/1.产品设计/[PROD_ORI]\<文档名\>.md`

步骤执行成功后，stdout 会输出 `OUTPUT_FILE=\<路径\>` 格式的一行。

带 `--enhance-content` 时，stdout 还会输出：

```text
ENHANCE_CONTENT=true
ENHANCE_INPUT=\<路径\>
```

此时 skill 必须继续执行 `enhance-content --input "\<OUTPUT_FILE\>"`。

**错误处理：**
- 未安装 `markitdown`：提示先安装依赖
- 输入文件不存在：提示检查路径
- 转换结果为空：提示检查源文件格式是否受支持

完成后输出：
```
✅ doc-to-md 完成！
文件：\<路径\>

⚡ 自动进入步骤二 enhance-content（图片分析与内容增强）...
```

---

## 独立工具：enhance-content（图片分析，步骤一的组成部分）

**触发词：** `enhance-content` 或"内容增强"或"图片分析"

> 正常流程中，enhance-content 在步骤一 doc-convert 完成后**自动执行**，无需单独触发。
> 仅当需要对已有 `[PROD_ORI]` 文件单独重新执行图片分析时，才显式触发本工具。

**职责：** AI 主导步骤，脚本只负责最终文件操作。

1. AI 读取 `image-classify-prompt.md` 作为判断规则上下文
2. AI 读取完整 `[PROD_ORI]` 文档，识别所有图片
3. AI 结合章节上下文 + 按需读取图片本身，判断每张图的类型和语义
4. AI 用 `write` 工具直接更新 `[PROD_ORI]`（将图片链接替换为语义化名称）
5. AI 构造 `--rename` 参数调用脚本，脚本直接执行重命名 + 生成过程记录

**执行：**

> ⚠️ **严禁直接执行以下命令**（不带 `--rename`/`--keep` 参数会触发旧的映射文件模式并报错）：
> ```bash
> # ❌ 错误：直接调用会因找不到映射文件而失败
> python .claude/skills/po-skills/run.py enhance-content --input "..."
> ```
>
> **✅ 正确做法**：必须先完成阶段 A（读规则）和阶段 B（逐图分析），再在阶段 C 构造带有 `--rename`/`--keep` 参数的命令执行。示例见阶段 C。

**AI 执行流程：**


### 阶段 A：读取规则和文档

用 `read` 工具依次读取：
1. `.claude/skills/po-skills/references/image-classify-prompt.md`（判断规则、命名规则、输出格式）
2. 输入的 `[PROD_ORI]` Markdown 文件（完整内容）

### 阶段 B：逐图判断

> ⚠️ **强制约束：先完成全部图片的上下文分析，再决定是否读图。禁止直接跳到读图。**

**B1. 对文档中每张图片，先仅凭上下文得出初步判断：**
- 可用信息：章节标题、图片所在表格的列头、附近文本（前后 2~3 行）、alt 文本
- 这些信息已包含在已读取的 `[PROD_ORI]` 文档中，**不需要再执行任何 read 命令**
- 对每张图输出初步结论：能判断 → 记录 `category/title/decision=renamed`；不能判断 → 标记「待读图」

**B2. 只有满足以下条件的图片，才补充读取图片本身（例外，非常规）：**
- 上下文只有弱描述（如「如下图」「见下图」），无法定位具体页面或流程
- 同一章节有多张相似图片，仅靠上下文无法区分
- 无法稳定判断图片类型或标题

> 🚫 **禁止行为**：不得在没有先完成上下文判断的情况下就读取图片；不得以「先读图确认类型」为由读图；不得逐张默认读取所有图片。

**B3. 补充读图后仍无法可靠判断 → 标记 `KEEP`，不强行命名。**

### 阶段 C：构造命令并执行脚本

AI 已在阶段 B 分析得到完整映射，直接构造 `--rename` / `--keep` 参数，**无需用 `write` 工具修改 [PROD_ORI]，脚本会自动更新图片链接**。

**分批调用规则（当 rename 总条数 > 20 时强制分批）：**

> ✨ **这是防止命令行过长导致脚本执行失败的核心机制。必须严格执行。**

- rename 条数 ≤ 20：一次性构造并执行，正常流程
- rename 条数 > 20：分批调用，每批不超过 **15 条**，自动连续调用直到全部完成

**小于 20 条时（正常流程）：**

```bash
python .claude/skills/po-skills/run.py enhance-content \
    --input "<PROD_ORI路径>" \
    --rename "./images/image2025-9-22_10-5-45.png" "./images/[原型图]客户列表页面-01.png" \
    --rename "./images/image2025-9-22_10-18-22.png" "./images/[原型图]客户信息编辑页-01.png" \
    --keep "./images/image2025-10-10_14-27-25.png"
```

**超过 20 条时（分批执行）：**

```bash
# 第 1 批：#1~#15
python .claude/skills/po-skills/run.py enhance-content \
    --input "<PROD_ORI路径>" \
    --rename "./images/old1.png" "./images/new1.png" \
    ... 共 15 条

# 第 2 批：#16~#30
python .claude/skills/po-skills/run.py enhance-content \
    --input "<PROD_ORI路径>" \
    --rename "./images/old16.png" "./images/new16.png" \
    ... 剩余条目
```

> ⚠️ **分批注意**：每批命令都使用同一个 `--input`。脚本有幂等性，已完成的批次重复执行时会自动跳过已重命名的文件。

**脚本一次性完成两件事：**
1. `images/` 目录下图片物理重命名
2. `[PROD_ORI].md` 中的图片链接同步更新（旧路径 → 新路径）

**输出：**
- 更新后的 `[PROD_ORI]` 文件（图片链接已替换）

**完成时推荐输出：**
```text
✅ enhance-content 完成！
文件：<路径>

处理结果：
- 共处理：X 张图片
- 已重命名：Y 张
- 保留原名：Z 张
- 处理失败：N 张
```

**降级规则：**
- 文档没有图片：直接跳过，不报错
- 无法判断某张图：`KEEP`，保留原文件名和原链接
- 某张图重命名失败：不中断整个步骤，在过程记录中标记 `处理失败`

完成后输出：
```
✅ enhance-content 完成！
文件：<路径>

处理结果：
- 共处理：X 张图片
- 已重命名：Y 张
- 保留原名：Z 张
- 处理失败：N 张
```

> ⚡ 接下来进入步骤三 story-analyze（Story 分析与规划）。
> **⚠️ 注意**：story-analyze 是纯 AI 步骤，`run.py` 中没有此命令，**严禁调用 bash**。
> 直接用 `read` 工具读取 `[PROD_ORI]` 文件 + `prd-convert-prompt.md`，输出三层结构分析表后等待用户确认。

---


## 步骤三：story-analyze（NL → [PROD_ORI] 末尾追加 Story 分析 + Story 规划）

**触发词：** `story-analyze` 或"需求分析"或"REQ_ANALYSIS_LIST"或"story规划"或"生成story规划"

**职责：** AI 核心步骤。读取 `[PROD_ORI]` 文件，按照 `prd-convert-prompt.md` 中定义的 Story/需求点/MUC 划分规则，识别三层结构，分配 key 编号，并将分析结果**追加到 [PROD_ORI] 文档末尾**，同时生成单独的 `[STORY_PLAN]`。

**⚠️ 重要**：此步骤必须严格按照下方流程执行，先读取所有参考文件，再按阶段 B 的表格格式输出结构分析。不要自行发挥输出格式。

**输入文件确定规则（按优先级）：**
1. 用户明确指定了文件路径 → 直接使用
2. 上一步处理的 `[PROD_ORI]` 文件 → 使用该文件
3. 用户提到了文档名称但未给路径 → 在项目根目录下各子目录中查找匹配的 `[PROD_ORI]*.md` 文件

**输出路径规则（强制）：** 三层结构分析直接追加到 `[PROD_ORI]` 文件末尾；`[STORY_PLAN].csv` 与 `[PROD_ORI]` 在同一目录下。

**AI 执行流程：**

### 阶段 A：读取文件

用 `read` 工具依次读取（**不要执行任何 bash 命令，必须全部读取后再进入阶段 B**）：
1. 输入的 `[PROD_ORI]` Markdown 文件
2. `.claude/skills/po-skills/references/prd-convert-prompt.md`（**必读**，包含 Story/需求点/MUC 的划分规则和 MUC 五维度识别方法）

> pmconfig 已在启动时加载，无需重复读取。

**如果未读取 `prd-convert-prompt.md`，将无法正确划分三层结构，必须先读取再继续。**

### 阶段 B：输出结构分析表，等待一次确认

读取完成后，**立即输出三层结构分析表，然后停下来等用户确认，不要自动继续**。

**输出前自校验（强制）：** 在输出结构分析表之前，AI 必须先执行以下校验，确保数据一致性：
1. **计数校验**：逐行扫描表格，分别统计唯一的 Story 数、唯一的需求点数、MUC 总行数，确保与汇总行的 `N 个 Story，M 个需求点，K 个 MUC` 一致
2. **层级完整性**：每个 Story 至少有 1 个需求点，每个需求点至少有 1 个 MUC
3. **格式一致性**：每行的"需求点类型"和"MUC 格式"列不能为空
4. 如果校验发现不一致，先修正再输出，不要输出有错误的表格

**三层结构分析表**
> ⚠️ **强制约束**：必须严格、原封不动地使用下方的 Markdown 表格形式进行输出！**绝对禁止**擅自改用嵌套的无序/有序列表（如 `Story > Feature > MUC`）进行简化展示。如果在这一步私自改换格式，将导致严重错误！

```
📋 结构分析：

输入文件：<路径>
输出：追加到 [PROD_ORI] 文档末尾

| L1 Story | L2 需求点 | L3 MUC | 需求点类型 | MUC 格式 | 变更类型 | 端侧 |
|----------|-----------|--------|------------|----------|----------|------|
| Story 1：<标题> | F-01 <标题> | MUC-01 <名称> | 混合型 | 格式一 | 新增 | 全栈 |
| | | MUC-02 <名称> | | 格式三 | 变更 | 前端 |
| | F-02 <标题> | MUC-03 <名称> | 同质型 | 格式二 | 新增 | 后端 |
| Story 2：<标题> | F-03 <标题> | MUC-04 <名称> | 混合型 | 格式四 | 变更 | 全栈 |

需求点类型说明：
  混合型 = MUC 之间差异大，需要 MUC 清单 + 逐个详述
  同质型 = 所有 MUC 同一类型，直接用统一格式表格，每行一个 MUC

MUC 格式说明：
  格式一 = 字段说明型（截图+字段表）
  格式二 = 功能点列表型（序号+触发条件+说明）
  格式三 = 功能变更型（原实现 vs 新实现对比）
  格式四 = 审批流程型（流程图+节点表）
  格式五 = 数据逻辑/业务规则型

共 N 个 Story，M 个需求点，K 个 MUC。

结构有误请说明；确认无误后，输入"继续"或"ok"生成文件。
```

### 阶段 C：用户确认后追加到 [PROD_ORI] 末尾

用户确认结构后，**立即执行以下操作，无需再次等待**：

**C1. 生成并冻结编号**：
- `story_key`：格式 `S-01`、`S-02`...（全局递增，此即 Story 的唯一标识，贯穿 PRD 和 Story 文档）
- `feature_key`：格式 `F-01`、`F-02`...（全局递增）
- `muc_key`：格式 `MUC-01`、`MUC-02`...（全局递增）
- 同一 Story/需求点/MUC 多行出现时复用原编号，不重新分配

**C2. 将三层结构分析追加到 [PROD_ORI] 文档末尾**：

用 `write` 工具在 `[PROD_ORI]` 文件末尾追加以下内容：

```markdown

---

## 附录：Story 结构分析（story-analyze）

> story_key 为 Story 的唯一标识（S-01、S-02...），在 PRD 和 Story 文档中统一使用。如后续执行 story-create，真实 DPMP ID 将替换此值。

| story_key | feature_key | muc_key | Story | 需求点 | MUC | 需求点类型 | MUC 格式 | 变更类型 | 端侧 |
|-----------|-------------|---------|-------|--------|-----|------------|----------|----------|------|
| S-01 | F-01 | MUC-01 | Story 1：<标题> | 需求点 1.1：<标题> | MUC-1：<名称> | 混合型 | 格式一 | 新增 | 前端 |
| S-01 | F-01 | MUC-02 | | | MUC-2：<名称> | | 格式三 | 变更 | 前端 |
```

### 阶段 D：输出结果

```
✅ story-analyze 完成！
- 三层结构已追加到 [PROD_ORI] 末尾：<路径>

共 N 个 Story，M 个需求点，K 个 MUC

⚡ 自动进入步骤三 prd-convert（生成结构化 PRD + EARS）...
```

---

## 独立工具：story-plan（可选 · 提取 PRD 生成建单表）

**触发词：** `story-plan` 或"生成建单表"或"生成story规划"

**说明：** 这是一个解耦的独立步骤，专门为 DPMP 自动化建单服务。它读取已经生成的 `[PROD_FORMAT]` 结构化需求文档，提取所有的 Story 标题，并生成 `[STORY_PLAN].csv` 文件供用户补充业务信息。

**前置条件：**
- `prd-convert` 步骤已完成，目录下存在 `[PROD_FORMAT]*.md` 文件。

**AI 执行流程：**

1. **读取文件**：读取输入目录下的 `[PROD_FORMAT]*.md` 和 `.claude/pmconfig.md`。
2. **提取 Story**：扫描 PRD 文件，找到所有以 `### X.X S-01：标题` 开头的标题行，提取 `story_key`（如 S-01）和标题。
3. **生成 CSV**：用 `write` 工具在同目录下新建 `[STORY_PLAN]<需求名>.csv`。
   表头严格如下：
   `story_key,story_id,story名称,story描述,所属完整迭代名,所属需求编号,所属需求名称,经办人工号,经办人姓名,创建人工号,计划开发完成日期,计划测试完成日期,计划完成日期`
   
   **填写规则**：
   - `story_key`：提取的 S-01
   - `story_id`：留空
   - `story名称`：读取 `pmconfig.md` 的"Story名称前缀"拼上标题
   - `story描述`：Story 的一句话描述
   - `所属完整迭代名`：读取 `pmconfig.md`，若无则填 `← 请填写`
   - `所属需求编号`：若已知则填，未知填 `← 请填写`
   - `所属需求名称`：当前需求标题
   - `经办人工号`/`经办人姓名`/`创建人工号`：读取 `pmconfig.md`，若无填 `← 请填写`
   - 日期列：留空

4. **完成提示**：
```
✅ [STORY_PLAN] 已生成：<路径>

请打开 CSV 文件，补充必要的业务字段（特别是 `所属需求编号` 和 `所属完整迭代名`）。
填写完成后，您可以输入 `story-create` 进行真实的 DPMP 创建。
```

---

## 独立工具：story-create（可选 · DPMP 批量创建 Story）

**触发词：** `story-create` 或"创建story"或"创建Story"或"批量创建story"

**说明：** 此步骤**不在主流程中**，可在任意时刻单独触发。主流程（story-analyze → prd-convert → req-review → story-split）使用 `story_key`（S-01、S-02）作为 Story 标识，文档已完整可用。当需要在 DPMP 系统中创建真实 Story 时，执行此工具——它会创建 Story 并将真实 DPMP ID **回写到所有已生成的文件中**（替换 S-01 等占位标识）。

**前置条件：**
- story-analyze 已完成，`[STORY_PLAN].csv` 已生成
- `.claude/skills/po-skills/.env` 中 DPMP_COOKIE 已配置

**输入文件确定规则（按优先级）：**
1. 用户明确指定了 `[STORY_PLAN]` 文件路径 → 直接使用
2. 当前需求目录下的 `[STORY_PLAN]*.csv` 文件 → 自动匹配

**AI 执行流程：**

### 阶段 A：检查 .env 配置

检查 `.claude/skills/po-skills/.env` 文件是否存在且包含 DPMP_COOKIE。如未配置，提示用户：
```
⚠️ DPMP 配置未找到。请在 .claude/skills/po-skills/.env 中配置：
DPMP_COOKIE=<从浏览器 F12 → Network → 任意 DPMP 请求 → Headers → Cookie 复制全值>
```

### 阶段 B：执行创建

```bash
python3 .claude/skills/po-skills/run.py story-create \
    --story-plan "<STORY_PLAN文件路径>"
```

**⚠️ 禁止通过 CLI 参数传递 Cookie**，所有 DPMP 配置由 `.env` 自动加载。

**幂等性说明**：已是真实 DPMP ID 的行自动跳过，中断后可安全重试。

### 阶段 C：全文件回写真实 ID

autodpmp 脚本在创建完成后，会**自动**从 `[STORY_PLAN].csv` 提取 `story_key → 真实DPMP_ID` 映射，并依次回写所有文件：

1. **[PROD_ORI].md**：替换末尾附录中 `story_key` 列的值（S-01 → 真实ID）
2. **[STORY_PLAN].csv**：新增 `story_id` 列，写入各 Story 的真实 DPMP ID（`story_key` 列 S-01 等保持不变）
3. **[PROD_FORMAT].md**：替换正文中的 `<!-- STORY_KEY: S-01 -->` 锚点和章节标题
4. **[STORY_FORMAT]文件重命名**：`[STORY_FORMAT][S-01]*.md` → `[STORY_FORMAT][真实ID]*.md`

### 阶段 D：完成汇总

将脚本输出的结果整理反馈给用户：
🎉 Story 创建完成！
- 成功创建：N 个
- 跳过（已存在）：M 个
- 失败：K 个

回写完成：
- [PROD_ORI]（附录三层结构）：✅
- [STORY_PLAN]：✅
- [PROD_FORMAT]：✅
- [STORY_FORMAT] 文件重命名：N 个 ✅

如有失败，检查错误信息后重新执行（已成功的自动跳过）。
```

---

## 步骤四：prd-convert（NL + [PROD_ORI] 附录结构 → 结构化 PRD + EARS）

**触发词：** `prd-convert` 或"需求结构化"或"生成PRD"

**职责：** AI 核心步骤。读取 `[PROD_ORI]` 文件（含末尾三层结构分析和 story_key），生成结构化 PRD 并在每个 MUC 详述后追加 EARS 描述块。PRD 中使用 story_key（S-01、S-02）作为 Story 标识。

**输入文件确定规则（按优先级）：**
1. 用户明确指定了文件路径 → 直接使用
2. 同目录下已有 `[PROD_ORI]` 文件 → 自动匹配
3. 用户提到了文档名称但未给路径 → 在项目根目录下各子目录中查找匹配的 `[PROD_ORI]*.md` 文件

**输出路径规则（强制）：** `[PROD_FORMAT]` 文件必须与输入的 `[PROD_ORI]` 文件在同一目录下（即 `1.产品设计/`）。

**AI 执行流程：**

### 阶段 A：读取文件

用 `read` 工具依次读取（**不要执行任何 bash 命令**）：
1. 输入的 `[PROD_ORI]` Markdown 文件（包含末尾的“附录：Story 结构分析”节，获取 story_key 和三层结构）
3. `.claude/skills/po-skills/references/prd-convert-prompt.md`
4. `.claude/skills/po-skills/references/prd-template.md`
5. `.claude/skills/po-skills/references/ears-rewrite-prompt.md`
6. `.claude/skills/po-skills/references/ears-syntax.md`（EARS 语法参考）

> pmconfig 已在启动时加载，无需重复读取。

### 阶段 B：按 [PROD_ORI] 末尾三层结构逐步生成

由于三层结构已在步骤二确认，此步骤不再需要用户确认结构，直接按 `[PROD_ORI]` 末尾附录的结构生成 PRD。

- 每步开始：`⏳ [X/N] 正在生成：<步骤名>...`
- 每步完成：`✅ [X/N] 完成，已写入文件`
- 每完成一步就用 `write` 工具将**当前全部已生成内容**写入输出文件

**PRD 锚点要求**：在每个 Story 章节标题中包含 `story_key`（从 [PROD_ORI] 末尾附录读取）：
```markdown
### 3.1 S-01：客户全景
<!-- STORY_KEY: S-01 -->
```
- Story 标题格式：`<story_key>：<标题>`
- `STORY_KEY` 注释填入 story_key（如 S-01；若已执行 story-create 则为真实 DPMP ID）

**需求点标题要求**：在每个需求点章节标题中包含 `feature_key`：
```markdown
##### 2.1.1 F-01：客户基本信息模块
```
- 需求点标题格式：`<feature_key>：<标题>`

**MUC 表格和详述要求**：MUC 清单表格中增加 `muc_key` 列，MUC 详述标题中包含 `muc_key`：
```markdown
| # | muc_key | MUC 名称 | 变更类型 | 端侧 | 备注 |
|---|---------|----------|----------|------|------|
| 1 | MUC-01 | 客户标签校验单元 | 新增 | PC | 简单，见备注 |
| 2 | MUC-02 | 客户标签状态控制器 | 修改 | 全端 | 见下方详述 |

**MUC-02 客户标签状态控制器 详述：**
```
- MUC 详述标题格式：`<muc_key> <MUC 名称> 详述：`

**EARS 追加要求**：在生成每个 MUC 详述内容后，紧接着在该 MUC 段落末尾追加 EARS 结构化描述块。EARS 块使用 `>` 引用格式：

```markdown
**MUC-02 客户标签编辑 详述：**

（... PRD 正文内容 ...）

> **EARS：**
> [Event-driven] When 用户点击"编辑标签"按钮，The System shall 弹出标签编辑弹窗。
> [State-driven] While 标签编辑弹窗处于打开状态，The System shall 允许用户添加、删除标签。
```

**EARS 转换规则**（详见 `ears-rewrite-prompt.md` 和 `ears-syntax.md`）：
- 仅对描述系统行为逻辑的内容进行 EARS 转换（触发条件、校验规则、状态控制等）
- 静态信息（字段说明表格、MUC 清单表格、审批节点表格、图片）不做 EARS 转换
- 每条 EARS 语句以 `[模式标签]` 开头：`[Ubiquitous]`、`[Event-driven]`、`[State-driven]`、`[Unwanted Behavior]`、`[Optional]`
- 语义不清晰无法转换的，标注 `<!-- EARS转换待确认：原文描述不够明确，建议 BA 补充 -->`

### 阶段 C：完成

```
🎉 PRD 生成完成！（含 EARS 结构化描述）
文件：<路径>
共 N 个 Story，K 个 MUC 已追加 EARS 描述

⚡ 自动进入步骤五 req-review（PRD 质量审查）...
```

**PRD 模板结构（参见 `references/prd-template.md`）：**
```
0. 基本信息（版本、PO、ARCH、BA 等）
1. 业务概览
   1.1 需求背景与业务目标
   1.2 关联需求
   1.3 用户与应用场景
   1.4 术语表
2. 功能需求
   一、用户职责和权限（角色矩阵）
   二、具体功能点
       2.1 <story_key>：Story 标题
       <!-- STORY_ID: <story_key> -->
           2.1.1 <feature_key>：需求点标题
               MUC 清单（含 muc_key 列）
               <muc_key> MUC 名称 详述（含 EARS 块）
           2.1.2 <feature_key>：需求点标题
           2.1.N 验收用例
       2.2 ...
3. 非功能需求（兼容性、性能、埋点）
4. 项目实施（外部依赖、发布计划、测试建议）
```

**输出：** 与输入 `[PROD_ORI]` 文件同目录下的 `[PROD_FORMAT]<文档标题>.md`



---

## 步骤五：req-review（需求质量审查与澄清）

**触发词：** `req-review` 或"需求澄清"或"需求审查"或"需求质量检查"

**职责：** AI 核心步骤。对 `[PROD_FORMAT]` 文档进行全维度质量检查，识别不一致点、逻辑不清晰点和待澄清点，输出审查报告供产品经理优化。

**输入文件确定规则（按优先级）：**
1. 用户明确指定了文件路径 → 直接使用
2. 上一步（prd-convert）刚刚生成了 `[PROD_FORMAT]` 文件 → 使用该文件
3. 用户提到了文档名称但未给路径 → 在项目根目录下各子目录中查找匹配的 `[PROD_FORMAT]*.md` 文件

**AI 执行流程：**

### 阶段 A：读取文件

用 `read` 工具依次读取（**不要执行任何 bash 命令**）：
1. 输入的 `[PROD_FORMAT]` Markdown 文件
2. `.claude/skills/po-skills/references/req-review-prompt.md`（需求审查规范）

> pmconfig 已在启动时加载，无需重复读取。

### 阶段 B：输出审查报告，等待产品经理处理

按 `req-review-prompt.md` 中的规范，对 `[PROD_FORMAT]` 文档逐章节进行 10 项检查，输出审查报告。
**⚠️ 强制要求**：严禁私自简化报告结构！无论检查是否全部通过，你都必须**完整输出以下全部结构（包括 10 项检查点的表格）**，不能有任何省略：

```
📋 需求质量审查报告：

输入文件：<路径>

### 需求规范校验

| 序号 | 检查点 | 是否通过 | 原因/说明 |
|------|--------|----------|-----------|
| 1 | 关联需求缺失 | ✅ / ❌ | [具体说明] |
| 2 | 功能入口路径缺失 | ✅ / ❌ | [具体说明] |
| 3 | EARS 核心要素缺失 | ✅ / ❌ | [具体说明] |
| 4 | 规则歧义 | ✅ / ❌ | [具体说明] |
| 5 | 异常缺失 | ✅ / ❌ | [具体说明] |
| 6 | 权限缺失 | ✅ / ❌ | [具体说明] |
| 7 | 约束缺失 | ✅ / ❌ | [具体说明] |
| 8 | 边界缺失 | ✅ / ❌ | [具体说明] |
| 9 | 状态缺失 | ✅ / ❌ | [具体说明] |
| 10 | 矛盾冲突 | ✅ / ❌ | [具体说明] |

校验结论：✅ 通过 / ❌ 不通过

### 待澄清需求

| 编号 | 需求章节 | 检查点类型 | 待澄清内容 | 优先级 |
|------|----------|------------|------------|--------|
| 1 | [章节标题] | [类型] | [描述] | 高/中/低 |

共 N 个待澄清项（高优 X 个，中优 Y 个，低优 Z 个）。

请根据审查结果优化 [PROD_FORMAT] 文档，完成后输入"继续"重新执行审查。
如暂不处理待澄清项，也可直接输入"跳过"结束流程（待澄清项会在 PRD 中标注 <!-- 待补充 -->）。
```

### 阶段 C：产品经理处理

- **产品经理选择优化**：修改 `[PROD_FORMAT]` 文件后输入"继续"，AI 重新执行审查（仅检查上次未通过的项）
- **产品经理选择跳过**：输入"跳过"，AI 记录待澄清项，在 `[PROD_FORMAT]` 的对应位置标注 `<!-- 待补充 -->`

### 阶段 D：完成

```
✅ 需求审查完成！
- 通过：X / 10 项
- 待澄清：Y 项（已在 PRD 中标注 <!-- 待补充 -->）

🎉 全部流程结束！
```

---

## 独立工具：quick-story（直接创建单条 DPMP Story）

**触发词：** `创建Story`、`新建Story`、`quick-story`

**职责：** AI 从用户的自然语言描述中提取必要字段，直接调用 DPMP API 创建单条 Story，无需 `[STORY_PLAN].csv`。

**必要信息（从用户描述中提取）：**
- Story 名称
- Story 描述
- 所属迭代名（如"2024-Q2-Sprint3"）
- 所属需求编号（如"TAILOR-124"）

**可选信息（缺失时从 pmconfig 或 .env 读取默认值）：**
- 经办人工号（默认读 `pmconfig.md` 中的"默认经办人工号"，或 `.env` 中的 `DPMP_DEFAULT_ASSIGNEE`）
- 创建人工号（默认同经办人）

**AI 执行流程：**

### 阶段 A：提取字段，确认信息

从用户描述中提取字段，读取 `.claude/pmconfig.md` 补全默认值，向用户展示确认：

```
📋 即将创建 Story：

名称：【前后端】客户标签管理
描述：支持对客户添加、删除、查询标签
迭代：2024-Q2-Sprint3
需求：TAILOR-124
经办人：012950（秦晓）
创建人：012950

确认创建请输入"确认"，或直接修改上述信息后再确认。
```

如果用户描述中缺少迭代名或需求编号，**必须先向用户询问，不要猜测**。

### 阶段 B：执行创建

用户确认后执行：

```bash
python3 .claude/skills/po-skills/run.py quick-story \
    --name "<Story名称>" \
    --desc "<Story描述>" \
    --iteration "<迭代名>" \
    --req-code "<需求编号>" \
    --assignee "<经办人工号>" \
    --reporter "<创建人工号>"
```

**Mock 模式（网络不通时）：**
```bash
python3 .claude/skills/po-skills/run.py quick-story \
    --name "<Story名称>" \
    --desc "<Story描述>" \
    --iteration "<迭代名>" \
    --req-code "<需求编号>" \
    --mock
```

### 阶段 C：完成

```
✅ Story 创建成功！
名称：<名称>
迭代：<迭代>
需求：<需求编号>
STORY_ID：<真实ID>
```

---

## 文件命名约定

| 前缀 | 含义 | 来源步骤 |
|------|------|----------|
| `[PROD_ORI]` | 自然语言原文，从 Wiki 转换，末尾包含 Story 三层结构分析附录 | 步骤一（doc-convert）+ 步骤二（story-analyze 追加附录） |
| `[STORY_PLAN]` | Story 规划 CSV（story_key + 名称/迭代/经办人等） | 独立工具（story-create 生成，可在主流程任意时刻单独触发） |
| `[PROD_FORMAT]` | 结构化 PRD，含 Story、功能点、EARS 描述 | 步骤三（prd-convert） |
| `[STORY_FORMAT][<story_key>]` | 单个 Story 的独立需求文档，放在 `1.产品设计/` 目录下 | 步骤三（prd-convert） |
